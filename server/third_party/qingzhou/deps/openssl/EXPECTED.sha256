# 打包进部署包的两个 OpenSSL DLL 的**期望字节**（N-23）。
# 为什么要有这个文件：N-20 只锁住了框架源码；这两个 DLL 决定部署包的密码哈希与 TLS 实现，
# 却来自"谁放了一份 / Git for Windows 升级到哪个版本"这种不受控的位置 —— 会静默漂移。
# 这里用**哈希**锁住（而不是把 6.5 MB 二进制提交进仓库）。
# build.ps1 解析到 DLL 后会比对，不一致会**显式告警**（并打印实际值），不会静默放过。
#
# 当前值来自 Git for Windows 3.5.7 的 mingw64\bin，与最初从轻舟 deps 拷的那份逐字节相同。
# 有意更换（例如升级 OpenSSL）时：改这里 + 在提交信息里说明。
0330b5f558996f297d687e1a2b2fcc2cacf883b16baef74aaef35285d7c1231c  libcrypto-3-x64.dll
feb5b300e0b3a021fed481178f3d66896426576d4d13b85a304d2a3809b25bfd  libssl-3-x64.dll