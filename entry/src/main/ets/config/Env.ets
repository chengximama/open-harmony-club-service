/**
 * 环境配置：联调期统一走后端 HTTP 8080。
 * - 模拟器里 127.0.0.1 指向模拟器自身，宿主机要用 10.0.2.2。
 * - 真机联调改成后端所在电脑的局域网 IP（如 http://192.168.x.x:8080/api/v1）。
 * - 后端切 HTTPS（serve-tls 8443）时，改协议和端口即可。
 */
export const BASE_URL: string = 'http://10.0.2.2:8080/api/v1';
