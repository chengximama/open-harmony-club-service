# D 同学模块（成员名录 + 成员详情）· 交付说明

> 对应设计规格 **P06 成员名录** / **P07 成员详情**，分工见仓库根目录 `FRONTEND_DIVISION(1).md` §🟣 D 同学详细任务。
> 本文档记录：功能概览 → 需求逐项核对（已实现 / 未实现）→ 使用说明 → 待决策清单。
>
> **核对基线**：`main` @ `e025064`（2026-09-17）；提交前已 rebase 到上游最新 `main` @ `5b7d09c`
> **构建结论**：`hvigorw --no-daemon assembleHap` → **BUILD SUCCESSFUL**（核对开始时本地基线的编译失败见 §6.1）

---

## 1. 模块概览

### 1.1 负责范围

| 项 | 内容 |
| --- | --- |
| 页面 | **成员名录**（P06）· **成员详情**（P07） |
| 定位 | 展示型页面：读多写少（卡片、分组、详情排版、权限隐藏按钮） |
| 核心产出 | 名录分组 · 详情排版 · 权限隐藏按钮 |
| 入口 | 底部 Tab「**名录**」（`Index.ets` 的第 4 个 Tab，索引 3）；点卡片后由 `Index.ets` 内部切换到详情视图 |

> 两个页面都**当组件用**（被 `Index.ets` import 后渲染），**不登记** `main_pages.json` —— 登记了反而会多出一套独立路由。这与 `docs/client-integration-review.md:201` 的结论一致。

### 1.2 文件清单

| 文件 | 状态 | 职责 |
| --- | --- | --- |
| `entry/src/main/ets/pages/MemberListPage.ets` | **重写**（原为静态壳，数据数组为空） | 名录：分组 / 搜索 / 计数 / 空态 |
| `entry/src/main/ets/pages/MemberDetailPage.ets` | **重写**（原全部为页内 mock 常量） | 详情：身份卡 / 基本信息 / 名下任务 / 三个写操作 |
| `entry/src/main/ets/components/MemberCard.ets` | **新建** | 复用成员卡片（分工文档要求的第三个文件） |
| `entry/src/main/ets/core/Theme.ets` | 增补（新增 4 个纯函数） | `roleLabel` / `memberStatusLabel` / `memberStatusFg` / `roleAvatarColor` —— 一处定义，名录与详情共用 |
| `entry/src/main/ets/pages/Index.ets` | 微改（2 处） | 给名录传 `refreshSignal`、给详情传 `onChanged` |
| `entry/src/main/ets/api/MemberApi.ets` | **未改**（依赖项） | 同名 `disable()` 重复声明曾阻塞全工程编译，**已由上游 commit `5b7d09c` 修掉**，详见 §6.1 |

### 1.3 接口清单

分工文档给 D 的 5 个接口**全部接线**：

| 接口 | 用途 | 调用位置 |
| --- | --- | --- |
| `GET /api/v1/members` | 名录（`status=active` 分页循环拉全；`q=` 搜索；`status=pending&size=1` 取待分配人数） | 名录页 |
| `GET /api/v1/depts` | 分组顺序 + 每组服务端人数 | 名录页 |
| `GET /api/v1/members/{id}` | 详情：`member` + `stats` | 详情页 |
| `PATCH /api/v1/members/{id}` | 编辑姓名（只传 `name`） | 详情页 |
| `POST /api/v1/members/{id}/disable` | 移出社团 | 详情页 |

**额外接线 2 个**（P07 设计稿要求，但不在 D 的 5 个接口清单里，均已在页面上标明归属）：

| 接口 | 为什么接 |
| --- | --- |
| `POST /api/v1/members/{id}/reset-password` | P07 的「重置密码 · 签发一次性临时密码」按钮。分工文档把它算在 **B 的管理页**；但静态壳里这个按钮已经存在，留着不打接口就是"假按钮"，且同一接口两页共用在服务端是幂等的。**已在页面上补上语义提示**（见 §3.3） |
| `GET /api/v1/tasks?owner_id={id}` | P07「查看某人负责的任务」（评审文档 §1.1 第 07 行列的就是 `GET /tasks?owner_id=`）。复用 C 的 `TaskApi.list()`，未新增客户端封装 |

---

## 2. 需求逐项核对

### 2.1 页面功能

| # | 分工文档要求 | 状态 | 实现说明 |
| --- | --- | --- | --- |
| 1 | 名录：按 4 个组织（主席团/课题部/运营部/宣传部）分组展示 | ✅ **已实现** | 分组**不写死**：顺序与名称取自 `GET /depts`（服务端 `seedDepartments` 按 `sort` 预置这 4 个，可改名，改名后客户端自动跟随） |
| 2 | 名录：点人看详情 | ✅ **已实现** | `MemberCard` 上抛 `memberId`，由 `Index.ets` 切换到详情视图 |
| 3 | 名录：搜索 | ✅ **已实现（服务端 `q`）** | 占位符 `搜索姓名或部门`；300ms 防抖；**服务端过滤**，客户端不做本地过滤 |
| 4 | 详情：查看某人负责的任务 | ✅ **已实现** | `GET /tasks?owner_id={id}`，未完成在前、已完成在后；每条带状态胶囊、部门、截止/逾期天数 |
| 5 | 详情：编辑姓名（权限受限） | ✅ **已实现** | `PATCH /members/{id}` 只传 `name`；按钮受 `can(user,'EditName',target)` 控制 |
| 6 | 详情：移出社团 | ✅ **已实现** | `POST /members/{id}/disable`；确认弹窗明确"账号与历史记录保留" |
| 7 | 主题 token 用 `core/Theme.ets`，页面不写死颜色 | ✅ **已实现** | 三个文件 **0 处硬编码色值**（已用 grep 校验）；新增的 4 个角色/状态取色函数也放在 `Theme.ets` |
| 8 | `components/MemberCard.ets`（复用成员卡片） | ✅ **已实现** | 原 `MemberCard` 是 `MemberListPage.ets` 内部的私有 struct，现已抽到 `components/` 并 `export` |

### 2.2 分工文档「必须记住的坑」四条

| # | 坑 | 状态 | 客户端做法 |
| --- | --- | --- | --- |
| 1 | **编辑姓名权限受限** → 403 `FORBIDDEN_ROLE` | ✅ | 没权限**直接不渲染按钮**，不靠报错兜底；`can()` 统一判定（含同权保护与"部长限本部门"） |
| 2 | **最后一个会长不能移出** → 409 `FORBIDDEN_LAST_PRESIDENT` | ✅ | 进详情页时用 `GET /members?role=president&status=active` 拉会长列表 → `isLastPresident()`；是唯一会长则**不渲染移出按钮**，并显示原因 |
| 3 | **名下有未完成任务** → 409 `MEMBER_HAS_OPEN_TASKS` | ✅ | ①身份卡与任务卡直接展示 `open_tasks`；②移出确认弹窗预列**最多 3 条**未完成任务；③真被拒时把错误码翻译成可执行提示 |
| 4 | **状态语义** active / pending / disabled，名录默认只显示 active | ✅ | 显式传 `status=active`（不依赖服务端默认值）；非 active 用状态胶囊标出；`disabled` 成员不再提供任何写操作 |

### 2.3 一周计划（Day 1–5）

| 天 | 任务 | 状态 |
| --- | --- | --- |
| Day 1 | 摸熟现有静态壳 | ✅ |
| Day 2 | 名录接真接口（`MemberApi.list()` + `DeptApi.list()`） | ✅ |
| Day 3 | 详情接真接口（`MemberApi.detail(id)` + 统计） | ✅ |
| Day 4 | 编辑姓名（`MemberApi.update(id, {name})`） | ✅ |
| Day 5 | 移出社团（`MemberApi.disable(id)` + 最后会长禁用判断） | ✅ |

### 2.4 P07 身份卡与统计字段

| 设计稿要素 | 状态 | 实现 |
| --- | --- | --- |
| `2025-09 加入 · 未完成 5 · 逾期 1 · 已完成 12` | ✅ | 全部取 `stats`（`joined_at` 截取 `YYYY-MM` + `open_tasks` / `overdue_tasks` / `done_tasks`）。逾期为 0 时该段自动省略，避免出现「逾期 0」的噪音 |
| 参与课题数 | ✅ | `stats.owned_plans` → 基本信息「负责课题」一行 |
| 手机号 | ❌ **有意不显示** | 服务端 `MemberBrief` 不下发 `phone`（`api-design.md §3.2`、评审 §6「名录不含手机号——这是隐私最小化」）。原来静态壳里的 `138****0000` 是假数据，已删；界面改为显式说明"不下发" |

---

## 3. 未实现 / 有意偏离的需求

> 以下都是**知道但没做**，逐条给原因，避免被误读成漏做。

### 3.1 P06 的「待分配」分组 —— 只做了计数，没做分组列表

| 项 | 说明 |
| --- | --- |
| 设计稿 | P06：`待分配单独成组并标出来源`，示例 `今天注册 · 来自招募链接（运营部预填）`；页眉 `42 位成员 · 4 个组织 · 待分配 3 人` |
| 已做 | 页眉的 `另有待分配 N 人（待管理员分配后进入名录）` —— 用 `GET /members?status=pending&size=1` 只取 `total`，**不传输名单** |
| 未做 | 待分配的**成员列表**与**来源标签** |
| 原因 | ① 来源需要 `dept_hint`，而它只在 `GET /members/pending` 里，该接口要求 `AssignPending` 权限（仅会长/副会长），名录却全社团可见 → 评审 **D-9（待决策：涉及最小化暴露面）**；② 组内还要显示 `registered_at`，`MemberBrief` 里没有；③ 分工文档明确「名录默认只显示 active，**别把 pending 混进来**」 |
| 建议 | 若保留设计稿做法，需服务端先决策 D-9；届时名录页加一段 `can(user,'ManageMembers')` 门控的 pending 分组即可（接口 `MemberApi.pending()` 已由 A 封装好） |

### 3.2 近千人规模的分页（评审 D-10）

| 项 | 说明 |
| --- | --- |
| 现状 | 服务端 `page/size` 默认 50、**上限 200**（`paging.cj:19,44`）；客户端用 `size=200` **循环拉全**，上限 `MAX_PAGES=20`（即 4000 人），到顶或不足一页就停 |
| 影响 | 百人级社团一次约 1–3 个请求，可接受；千人级会有 5+ 个请求且全部驻留内存（`ForEach` 一次性构建，未用 `LazyForEach`） |
| 建议 | 服务端提供"分组 + 组内分页"接口，或客户端改 `LazyForEach` + 按组懒加载。**不建议**改用本地过滤 —— 分页上限决定了本地拿不全 |

### 3.3 临时密码语义（评审 D-12）—— 界面文案按服务端实况修正

| 项 | 说明 |
| --- | --- |
| 原静态壳文案 | `请立即告知成员，首次登录后会强制改密。` |
| 问题 | 服务端**没有**"首登强制改密"标志（`h_secret.cj:89`，评审 D-12）：口令只在响应里回一次，但之后长期有效。原文案是**错的**，会让管理员误以为系统已兜底 |
| 现文案 | `该口令只在本次响应里出现，之后看不到；原密码已失效。请立即告知本人，并提醒尽快在「我的」页自行修改密码。` |
| 归属 | 文案修正（客户端）；若要"真一次性"，需服务端加标志，v1 不做 |

### 3.4 唯一会长判定的一个边界（有意选择 fail-safe）

| 项 | 说明 |
| --- | --- |
| 服务端口径 | `presidentCount` 统计 `role=='president' && status!='disabled'`（含待分配态） |
| 客户端口径 | 查询 `?role=president&status=active`，只统计 **active** 会长 |
| 差异 | 仅当存在"会长 + pending"这种数据时才会分歧（正常流程下不可能出现）。此时客户端会**更保守** —— 少显示一个移出按钮，而不会多显示一个会被 409 拒绝的按钮。破坏性操作上这是正确方向，故不修 |
| 备注 | 客户端**不做**"自己算一遍角色档位"的事，权限判定只有 `PermissionStore.can()` 一个入口 |

### 3.5 详情页不含"改部门 / 改角色"

| 项 | 说明 |
| --- | --- |
| 分工文档 | D 只负责「编辑姓名（权限受限）」 |
| 现状 | `PATCH /members/{id}` 的 `dept_id` / `role` 走的是另一套权限（仅会长/副会长），且角色枚举里**没有「会长」**（只能由会长移交产生）—— 属于 B 的管理页 |
| 结论 | 详情页只发 `name` 字段。`MemberApi.update()` 已支持另外两个字段，B 或后续扩展可直接复用 |

---

## 4. 使用说明

### 4.1 联调前置

```powershell
# 1. 后端（在 server/ 下）
powershell -NoProfile -ExecutionPolicy Bypass -File .\build.ps1
cd build
.\club-server.exe init-admin 13800000000 YourPass123 dev-data
.\club-server.exe serve 8080 dev-data        # 首次启动自动预置 4 个组织
```

```ts
// 2. 客户端基地址：entry/src/main/ets/config/Env.ets
const DEV_BASE_URL: string = 'http://10.0.2.2:8080/api/v1';   // 模拟器 → 宿主机
// 真机联调改成后端电脑的局域网 IP，例如 'http://192.168.1.100:8080/api/v1'
```

> `10.0.2.2` 只适用于模拟器访问宿主机。真机请填局域网 IP，否则名录会一直停在"网络不可用"。

### 4.2 逐项验证（含期望看到的确切文字）

| 验证点 | 操作 | 期望 |
| --- | --- | --- |
| 名录分组 | 登录会长账号 → 底部 Tab「名录」 | 标题 `成员名录`；副标题形如 `4 位成员 · 2 个组织`，右侧 `全社团可见`；按 `主席团 / 课题部 / 运营部 / 宣传部` 顺序分组，每组右侧 `N 人` |
| 搜索 | 搜索框输入 `运营` | 副标题变为 `「运营」命中 N 位 · 1 个组织`，分组只留命中项；输入框右侧出现 ✕ 可清空 |
| 名称搜索 | 输入某成员姓名的连续片段 | 按**姓名模糊匹配**返回（服务端 `q` 同时匹配姓名与部门名，大小写不敏感，**不匹配手机号**） |
| 隐私约束 | 翻到名录底部 | `名录不含手机号——这是隐私最小化` |
| 空态 | 搜索一个不存在的词 | `没有匹配「xxx」的成员，换个姓名或部门名试试。` |
| 进详情 | 点任一成员卡片 | 顶部身份卡：首字头像 + 姓名 + `部门 · 角色` + `YYYY-MM 加入 · 未完成 N · 逾期 N · 已完成 N`（逾期为 0 时该项不出现） |
| 基本信息 | 详情页「基本信息」卡 | `部门 / 角色 / 加入时间 / 状态 / 负责课题` 五行 + 尾注 `手机号属于个人信息，名录与详情均不下发 —— 这是隐私最小化。` |
| 名下任务 | 详情页「负责的任务」卡 | 右上 `共 N 项 · 未完成 N`；未完成在前带状态胶囊；逾期条目**标题与截止行变红** |
| 编辑姓名 | （有权限时）点「编辑姓名」→ 输入新名 → 保存 | 弹窗 `当前：{原姓名} · {角色}`，输入框占位 `输入新姓名`；成功后 Toast `姓名已更新`，身份卡与名录同步刷新；空名提交时弹窗内红字 `姓名不能为空` |
| 重置密码 | （有权限时）点「重置密码」→ 确认重置 | 确认页 `将为 {姓名} 签发新的临时密码，其原密码立即失效。确认重置？`；随后出结果页 `已签发临时密码` + **服务端返回的**临时密码 + 提示自行改密 |
| 移出社团 | （有权限时）点「移出社团」 | 确认页说明"账号与历史记录全部保留，日后可由管理员恢复"；名下有未完成任务的，弹窗内**红底块**列出最多 3 条并提示 `名下还有 N 项未完成任务，移出社团前需先转交。` |
| 移出成功 | 确认移出 | Toast `已移出社团（账号与历史记录保留）`；返回名录后该成员消失 |
| 最后会长保护 | 用会长账号看**自己**的详情 | 不出现「移出社团」按钮；提示 `TA 是社团唯一在任的会长 —— 社团必须始终保留至少一位会长，不能移出。`（见 §4.3 说明） |
| 权限隐藏 | 用普通成员账号看别人的详情 | 三个按钮全部不出现，只显示 `你对这位成员没有可执行的操作。` |

### 4.3 权限矩阵（按钮显隐，全部由 `PermissionStore.can()` 推导）

| 当前用户角色 | 编辑姓名 | 重置密码 | 移出社团 |
| --- | --- | --- | --- |
| 会长 | ✅ 任何人（含自己） | ✅ 除自己外任何人 | ✅ 除自己、且对方不是唯一会长 |
| 副会长 | ✅ 部长/副部长/成员 | ✅ 同上 | ✅ 同上 |
| 部长 / 副部长 | ✅ 本部门的更低档位（含自己） | ✅ 本部门更低档位 | ✅ 本部门更低档位 |
| 普通成员 | ❌ | ❌ | ❌ |

> 「自己」这一列看起来严于直觉，但**是服务端口径**（P07 原文：`不能对同权限或更高权限的人执行上述操作，会长也不能重置自己`；`perms.cj` 的 `checkAccess` 在动作语义层统一拦 `ResetPassword / SetRole / DisableMember / EditMemberName`）。
> 因为"唯一会长"的判定里会长是最高档，自己对自己的移出永远落在「最后会长保护」上 —— 想移出会长，只能先在管理页做**会长移交**，这与产品规则一致。

### 4.4 常见返回码在界面上的表现

| 返回 | 界面表现 |
| --- | --- |
| `401`（token 失效） | 全局：清登录态 → Toast「登录已过期，请重新登录」→ 回登录页（`HttpClient` 统一处理） |
| `403 FORBIDDEN_ROLE` | 全局 Toast「不能对同权限或更高权限的人执行此操作」；正常路径下按钮已隐藏，只有并发/直接调用才会看到 |
| `403 FORBIDDEN_NOT_IN_DEPT` | Toast「只能操作本部门范围内的人与事」 |
| `409 FORBIDDEN_LAST_PRESIDENT` | Toast「社团必须始终保留至少有一位会长」；正常路径下按钮已隐藏 |
| `409 MEMBER_HAS_OPEN_TASKS` | 弹窗内红字提示「名下还有 N 项未完成任务，移出社团前需先转交。」 |
| `400 VALIDATION_FAILED`（姓名为空） | 弹窗内红字「姓名不能为空」（与后端 `fields.name` 文案一致） |
| 网络不可用 | Toast/错误块「网络不可用，请检查服务端是否可访问」+「重试」按钮 |

---

## 5. 与既有评审文档的差异（有一处需要同步）

| 评审条目 | 评审时的状态 | 现在的实际状态 |
| --- | --- | --- |
| **D-2 成员名录没有「搜索姓名或部门」** | 服务端 `GET /members` 无关键词参数；客户端只是写死的 `🔍 搜索成员` | ⚠️ **服务端已于 2026-09-16 实现 `q`**（`h_member.cj:59-110`，姓名 OR 部门名，大小写不敏感，**手机号不参与匹配**），`api-design.md §3.2` / `frontend-brief.md` 也已同步。客户端本次接线完成 → **D-2 可以关闭**，建议在 `docs/ui-spec-conformance-review.md` 里更新该条状态 |
| D-7 成员详情计数 | 服务端 `stats` 缺 `done_tasks` / `overdue_tasks` | 服务端已补齐，客户端本次直接消费 → **可关闭** |
| D-9 待分配来源 | 待决策 | 仍未决策，客户端按 §3.1 处理（只计数） |
| D-10 名录分页 | 建议服务端给分组接口 | 未变，客户端按 §3.2 循环拉全 |
| D-12 临时密码语义 | 建议文档写明 | 客户端界面文案已按实况修正（§3.3） |

---

## 6. 本次改动文件清单与关键修复

### 6.1 编译阻塞：`MemberApi.ets` 的重复 `disable()`（已由上游同步修掉）

本次核对开始前，本地克隆（`main` @ `e025064`）的基线构建是**失败**的：

```
> hvigor ERROR: ArkTS Compiler Error
1 ERROR: 10505001 ArkTS Compiler Error
Error Message: Duplicate function implementation. At File: .../api/MemberApi.ets:147:9
2 ERROR: 10505001 ArkTS Compiler Error
Error Message: Duplicate function implementation. At File: .../api/MemberApi.ets:167:9
COMPILE RESULT:FAIL {ERROR:3 WARN:67}
> hvigor ERROR: BUILD FAILED in 20 s 171 ms
```

`MemberApi.ets` 里 `disable()` 被声明了两次（两次分支合并的产物），**ArkTS 对类内重复实现是硬 ERROR** ——
即不只是 D 的页面，**任何客户端构建都过不去**。

> **2026-09-17 20:26 追记**：上游已用同样方式修掉（commit `5b7d09c`「fix(arkts): 移除 MemberApi 重复的 disable
> 方法（合并冲突引入）」，作者 Zephyr）。
> 因此本次提交**不再包含该改动**，`MemberApi.ets` 与上游逐字节一致（保留 `disable()` 一份）。
> 两条修复唯一区别是保留的是哪一份（上游保留「有未完成任务会拒绝」那条注释），功能完全等价。

### 6.2 文件级改动（本次提交）

| 文件 | 改动 |
| --- | --- |
| `core/Theme.ets` | 新增 `roleLabel()` / `memberStatusLabel()` / `memberStatusFg()` / `roleAvatarColor()`（4 个纯函数，无色值硬编码，名录与详情共用） |
| `components/MemberCard.ets` | **新建**：头像圈 + 姓名 + `部门 · 角色` + 非 active 状态胶囊 + chevron，`onCardClick` 上抛 |
| `pages/MemberListPage.ets` | **重写**：真接口 + 服务端搜索 + 分页循环 + 页头三段计数 + loading/error/empty + `@Prop @Watch refreshSignal` + 部门表失败降级 |
| `pages/MemberDetailPage.ets` | **重写**：`detail`/`tasks`/`presidents` 三路加载 + 权限驱动按钮 + 三个写操作接线 + 4 个自包含弹窗 + 内联错误 + 防重复提交 |
| `pages/Index.ets` | 名录传 `refreshSignal`；详情传 `onChanged`（改名/移出后名录自动刷新） |
| `docs/d-member-module.md` / `README.md` | 本文档 + 文档索引补一行 |

> `api/MemberApi.ets` **不在本次改动里** —— 见 §6.1。

### 6.3 构建验证

```
$ hvigorw --no-daemon assembleHap
> hvigor BUILD SUCCESSFUL in 26 s 34 ms
```

---

## 7. 待决策 / 后续项（汇总给产品与服务端）

| # | 事项 | 归属 | 影响 |
| --- | --- | --- | --- |
| 1 | **D-9**：名录「待分配」组的来源标签是否暴露给普通成员 | 产品 + 服务端 | 决定 P06 分组要不要补；当前只做了人数计数 |
| 2 | **D-10**：近千人规模的名录要不要服务端分组/组内分页接口 | 服务端 | 当前客户端循环拉全（上限 4000 人） |
| 3 | **D-12**：临时密码要不要加"首登强制改密"标志 | 产品（v1 建议不做） | 当前界面已如实说明"无强制改密" |
| 4 | **D-8**：待分配列表排序方向（最新在前 vs 最早已在前） | 产品 | 影响 B 的待分配审批页，不影响本模块 |
| 5 | 同步 `docs/ui-spec-conformance-review.md`：D-2 / D-7 可标记为已解决 | 文档 | 避免后续按过期结论重复排查 |
