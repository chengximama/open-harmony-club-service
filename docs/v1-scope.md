# 社团管理工具 v1 范围说明

- 状态：草案 v0.10（待评审冻结）
- 日期：2026-09-11
- 用途：社团内部试用（第一阶段）
- 修订记录：
  - v0.2 依甲方《组织与角色配置表》更新角色预设、数据模型与待确认项
  - v0.3 甲方答复 3 个范围问题：**③课题容器进入 v1**（范围扩大）、**④移除成果字段**、**⑤不做审核流程**（范围缩小）
  - v0.4 登录账号改为**手机号**（社团含校外人员，不用学号）；**注册与授权解耦**（注册后为 `pending`，角色全部由会长授予，换届只改角色不动账号）；采用**框架原生 TLS**；第 2 节验收标准 1 随之修订
  - v0.5 确认术语为「**课题**」；首任会长改用**初始化命令**预置；课题由「两级」改为**可层层往下分割的多级结构**（范围扩大，见 3.5.2）
  - v0.6 依 Part 3 评审：**删除 `Department.lead_id`**（部长身份只由 `Member.role` 表达）；**副会长与会长同权**（除移交会长）；**成员名录全社团可见**；**注册口令存明文**
  - v0.7 依 Part 4 评审：**删除 `Task.calendar_event_id`**，日历同步改为**客户端本地机制**（见第 7 节）；任务改为**软删除**
  - v0.8 依 Part 5 评审：**`Plan.dept_id` 只保留在顶层课题**（子课题部门由根决定）；课题树**深度硬上限 6 层**；禁止课题跨部门移动
  - v0.9 依 Part 6 交叉复查补全两处缺口：页面清单 **9 → 11 页**（补登录页、我的/个人设置）；权限矩阵**补 3 行**（编辑/删除任务、编辑课题、移动/删除课题）
  - v0.10 **服务器确认为 Windows**（与开发机同平台）：改为**本机编译 + 拷贝制品**部署，服务器无需装编译器；原 Linux 相关约束全部作废（Doc: `deploy-windows-verify.md`）
  - v0.11 **服务端 M1 实现期间的三处修订**（2026-09-13）：① **部门增删改改为会长独占**（原矩阵副会长一列为 ✅，与 `api-design` §3.1/§6.1「DELETE 限会长」冲突，已按更严格一侧统一）；② `Member` 增加 **`dept_hint`** 字段（招募链接的部门预填需落库，否则 `GET /members/pending` 的 `dept_hint` 无从返回）；③ 明确**会长只能由「移交会长」产生**（规则 11 补充说明，见 §6）

---

## 1. 要解决的两个问题

1. 社团里有哪些人
2. 每件事由谁负责、做到什么程度

**关键判断：这两个不是并列的功能，而是同一问题的静态面与动态面。**

名录是任务的目录，真正让人每天打开的是「我负责什么」。因此：

- 首页 = **我的任务**，不是组织架构图
- 若后续必须削减，先砍名录的花哨功能，**绝不砍个人任务视图**

> 内部工具最经典的失败模式：管理员建完部门和角色，日常协作仍回到微信群，工具在两周后变成一个没人更新的花名册。

---

## 2. 试用成功标准（v1 验收）

试用期定为 2 周，结束时逐条对照：

| # | 验收标准 | 判定方式 |
| --- | --- | --- |
| 1 | 新成员从**注册到被分配部门** < 2 分钟，且会长**一次操作可覆盖一批人** | 实测新成员 3 人 + 一次批量分配 |
| 2 | 任务从创建到负责人看到 < 1 分钟 | 实测 |
| 3 | **课题下的任务能被一眼看清进度**（见第 6 节规则 7） | 抽查 3 个课题 |
| 4 | 部门群里「这事谁负责」明显变少 | **真 KPI** |

**第 4 条不达标即视为失败，功能再全也不算通过。**

> **v0.4 修订说明**：原标准为「新成员从拿到邀请码到看到自己部门 < 2 分钟，**且管理员无需手动录入**」。
> 改用「先注册、后授权」模型后，每个新成员都必须经会长分配，**“无需手动录入”不再成立**。
> 因此标准改为：可批量达成，且**一次操作能覆盖一批人**——否则招新期会长会成为瓶颈，
> 而招新期恰恰是本工具最该发挥作用的时刻。

---

## 3. v1 范围

### 3.1 做（原定 7 项 + 1）

创建部门 · **注册与授权** · 设置角色 · 创建任务 · 分配负责人 · 更新状态 · 提醒截止时间
· **创建课题并挂载任务**（v0.3 新增，见 3.4.4 ③）

> v0.4 把原「邀请成员」改为「**注册与授权**」：注册（凭社团共享口令，得 `pending` 账号）与授权（会长分配部门+角色）是两件独立的事，后者是 v1 必须做的核心。

### 3.2 明确不做

写下来是为了防止开发中途扩大范围：

- 会议、知识库（第二 / 第三阶段）
- 群聊、私信、文件传输
- **成果提交与附件上传**（甲方答复④：完成即可，不需要提交物）
- **审核流程 / 「待审核」状态**（甲方答复⑤：暂时掠过）
- 统计报表、数据看板
- 多级部门树（v1 只做单层）
- **任务分层**：任务永远是最小执行单元，**没有子任务**。可分层的是课题（见 3.3 决策表），别把两者搞混
- 自定义角色与权限矩阵（v1 只做甲方定义的 **5 个固定预设**）
- 任务进度百分比

> 注：原「项目容器放第二阶段」一条已删除——甲方答复③确认的这个"大计划"**就是**项目容器，已提前进入 v1。**同一个东西不留两个名字。**

### 3.3 已确认决策

| 决策项 | 结论 | 说明 |
| --- | --- | --- |
| 客户端 | **鸿蒙 App 优先** | 沿用当前 `cangjie_web` 工程方向 |
| 角色 | **固定 5 档预设，只做分配** | 依甲方配置表，不做角色权限编辑器，见 3.4 |
| 账号模型 | **手机号登录 + 注册与授权解耦** | 注册后为 `pending`、无任何权限；部门与角色全部由会长授予。**换届、毕业、返校都只改角色，账号不动** |
| 分层 | **课题可层层往下分割（多级）→ 任务是叶子** | 一个课题下可挂**子课题**，任意层；任务的父节点可以是任意层级的课题。**任务本身不再分层** |
| 提醒 | **应用内高亮 + 加入系统日历** | 绕开后台推送通道，见第 7 节 |
| 服务端 | **自建，部署在老师提供的云服务器** | **Windows**（与开发机同平台） |
| 后端技术栈 | **仓颉 1.1.3 + 轻舟框架** | 学习目的之一，接受生态不确定性 |
| 部署方式 | **本机编译，拷贝制品到服务器** | 同平台 ⇒ 服务器**无需安装编译器**；制品为 exe + 4 个 DLL，见 `deploy-windows-verify.md` |
| 持久化 | **文件存储（JSON 落盘）** | 仓颉生态无可用 SQLite 驱动，见第 8 节 |

### 3.4 组织与角色预设（依甲方配置表）

#### 3.4.1 甲方原表（权威定义，照录）

| 序号 | 所属组织 | 角色预设 | 人数设置 | 主要权限 |
| --- | --- | --- | --- | --- |
| 1 | 主席团 | 会长 | 1 人 | 管理全部部门和成员，查看全社团任务与项目，设置管理角色 |
| 2 | 主席团 | 副会长 | 2-3 人 | 协助会长管理社团，查看和协调各部门工作 |
| 3 | 课题部 | 部长 | 1 人 | 管理本部门成员，创建和分配部门任务，发起及跟进课题 |
| 4 | 课题部 | 副部长 | 1-2 人 | 协助部长管理成员、任务和课题 |
| 5 | 课题部 | 成员 | 可多人 | 接收和完成任务，参与课题，提交进度及成果 |
| 6 | 运营部 | 部长 | 1 人 | 管理本部门成员，安排活动运营及日常工作 |
| 7 | 运营部 | 副部长 | 1-2 人 | 协助部长进行活动筹备、任务分配和进度跟进 |
| 8 | 运营部 | 成员 | 可多人 | 接收和完成运营任务，反馈工作进度 |
| 9 | 宣传部 | 部长 | 1 人 | 管理本部门成员，安排文案、设计、摄影和新媒体任务 |
| 10 | 宣传部 | 副部长 | 1-2 人 | 协助部长分配宣传任务、审核内容和跟进进度 |
| 11 | 宣传部 | 成员 | 可多人 | 完成文案、设计、摄影、排版等宣传任务 |

#### 3.4.2 归纳结果

- **组织（4 个，单层）**：主席团 · 课题部 · 运营部 · 宣传部
- **角色预设（5 个）**：会长 · 副会长 · 部长 · 副部长 · 成员

> 主席团是**组织单元**，会长/副会长归属其中，但权限覆盖全社团。

#### 3.4.3 v1 权限矩阵（由甲方描述推导）

| 能力 | 会长 | 副会长 | 部长 | 副部长 | 成员 |
| --- | --- | --- | --- | --- | --- |
| 查看**成员名录** | 全社团 | 全社团 | 全社团 | 全社团 | **全社团** |
| 查看**任务 / 课题** | 全社团 | 全社团 | 本部门 | 本部门 | 本部门 |
| 编辑成员姓名 | ✅ | ✅ | 本部门、档位更低 | 本部门、档位更低 | — |
| 调部门 / 改角色 | ✅ | ✅ | — | — | — |
| 移出社团 | ✅ | ✅ | — | — | — |
| 审批待分配 / 批量分配 | ✅ | ✅ | — | — | — |
| 重置成员密码 | ✅ | ✅ | 本部门、档位更低 | 本部门、档位更低 | — |
| 部门增删改 | ✅ | — | — | — | — |
| 注册口令 | 查看 + 更换 | 仅查看 | — | — | — |
| 招募链接管理 | ✅ | ✅ | — | — | — |
| 创建课题 | ✅ | ✅ | 本部门 | 本部门 | — |
| 创建 / 分配任务 | 全部 | ✅ | 本部门 | 本部门 | — |
| 更新任务状态 / 阻塞原因 | ✅ | ✅ | ✅ | ✅ | 自己的任务 |
| 编辑 / 删除任务 | ✅ | ✅ | 创建人或本部门 | 创建人或本部门 | — |
| 编辑课题内容 | ✅ | ✅ | 负责人或本部门 | 负责人或本部门 | 仅作为负责人 |
| 移动 / 删除课题 | ✅ | ✅ | 本部门 | 本部门 | — |
| **移交会长** | ✅ | — | — | — | — |
| 「我的任务」首页 | ✅ | ✅ | ✅ | ✅ | ✅ |

> **v0.9 补 3 行**：原矩阵只有「创建课题」「创建/分配任务」「更新任务状态」，
> 但接口设计里还定义了**编辑/删除任务、编辑课题、移动/删除课题**的权限。
> 不补这几行，实现时这几处会各自为政——而权限不一致是最难在测试中发现的一类缺陷。

> **v0.6 修订**：副会长由「不能管理成员」改为**除「移交会长」与「改动会长本人」外，与会长同权**。
> 原表把副会长的成员管理标为 `—`，与甲方"协助会长管理社团"的描述不符。
> 另：**成员名录改为全社团可见**——这是第 1 节第一个问题「社团里有哪些人」的答案；
> 而任务与课题仍按部门限制，两者可见性不必一致。
>
> **v0.11 修订（2026-09-13）**：副会长的例外从两条扩为**四条**——「移交会长」「改动会长本人」
> 「更换注册口令」「部门增删改」。前两条是 v0.6 定的；注册口令是 `api-design` §3.6 已有规定
> （副会长仅查看）；**部门增删改改为会长独占**是本次实现期间发现的文档冲突的裁定结果
> （§3.4.3 原写 ✅，而 `api-design` §3.1/§6.1 写「DELETE 限会长」）。理由：组织架构是社团层面的事，
> 增删改会影响所有人的归属；副会长可以**管人**（调部门、改角色），但不直接改动组织本身。
>
> **v0.12 修订（2026-09-14，代码评审后）**：本表两处口径与实现对齐。
> 1. 「重置成员密码」的成员列由「仅本人」改为 `—`：`api-design` §3.5 正文与 §6.1 都写"不含本人"，
>    只有本表与 §3.8 写「仅本人」，属文档自相矛盾。裁定为**管理员救济动作**，本人改密走 `PUT /auth/password`。
> 2. 「编辑成员姓名」与「重置成员密码」对部长 / 副部长追加**档位限制**：目标档位必须低于自己
>    （会长 &gt; 副会长 &gt; 部长 = 副部长 &gt; 成员）。原实现只保护了"副会长不能改会长本人"，
>    同部门的部长因此可以重置会长密码并接管账号（P0 缺陷，详见 `code-review.md` H-1）。
>    例外：部长可以改**自己的**姓名（改名不改变角色，不构成提权）。

**v1 系统级别映射**（实现用）：

| 角色 | 系统级别 | 一句话 |
| --- | --- | --- |
| 会长 | 超级管理员 | 全部权限 + 设置角色 |
| 副会长 | 社团管理员 | **除「移交会长」与「改动会长本人」外，与会长同权**（v0.6） |
| 部长 | 部门管理员 | 管本部门成员 + 创建分配本部门任务与课题 |
| 副部长 | 部门管理员 | **与部长同权**（甲方答复①，见 3.4.4） |
| 成员 | 普通成员 | 看本部门，更新自己任务的状态与阻塞原因 |

#### 3.4.4 甲方答复（2026-09-11）

| # | 问题 | 甲方答复 | 对 v1 的影响 |
| --- | --- | --- | --- |
| ① | 副部长与部长的权限边界 | 未特别区分 | 按建议：**两者同权**，仅名称区分 |
| ② | 人数设置是否硬校验 | 未特别要求 | 按建议：**只做软提示**，不做硬限制 |
| ③ | 「课题」是否必须有容器 | **"用一个大的计划下挂多个小任务"** | ⚠️ **确认需要 → 容器进入 v1**，成为 v1 第 8 项功能 |
| ④ | 「成果」是什么形态 | **"只需要标注完成即可"** | **不做成果提交**，`Task` 不设 result 字段 |
| ⑤ | 宣传部「审核」 | **同④，暂时掠过，只标注完成** | **不做审核流程**，状态保持 4 档 |

> **命名说明（已确认）**：术语定为「**课题**」（沿用甲方表格用词），字段名统一用 `plan`。如日后要改称呼，改的只是显示文案，不影响数据模型。

### 3.5 范围变更评估（依文末《范围变更规则》）

#### 3.5.1 v0.3：课题容器进入 v1

③ 属于**新增功能**，按规定须评估。结论：

| 维度 | 评估 |
| --- | --- |
| 是否服务于第 1 节的两个问题 | ✅ 直接服务于"每件事由谁负责、做到什么程度"——课题是任务的组织单元 |
| 是否引入新权限模型或新外部依赖 | ❌ 不引入。权限仅复用现有部门管理员级别 |
| 是否推迟验收标准 | ❌ 不推迟。仅增加一项目标（验收标准第 3 条） |
| **工作量影响** | **+1 张表、+2 个页面、+1 个外键字段，约 +20~25%** |

**裁定：准予进入 v1。** 理由：④⑤ 两项范围缩减（去掉成果提交与审核流程）抵消了相当一部分增量，净增量可控。

#### 3.5.2 v0.5：课题改为多级嵌套

| 维度 | 评估 |
| --- | --- |
| 是否服务于第 1 节的两个问题 | ✅ 服务于"每件事由谁负责、做到什么程度"——大课题确实需要层层拆分才看得清 |
| 是否引入新权限模型或新外部依赖 | ❌ 不引入。子课题沿用父课题的部门与权限 |
| 是否推迟验收标准 | ❌ 不推迟。第 2 节验收标准 3 的抽查范围从"课题目录"扩展到"课题树" |
| **工作量影响** | **+1 个自引用字段**，但真正的增量在：递归进度聚合、树形 UI、环形校验、删除上提 —— 约 **+15%** |

**裁定：准予进入 v1。** 数据模型增量很小（自引用 `parent_id`），复杂度集中在四处逻辑上，且这四处**都是必须做对的**，不能打折：

| # | 必须做对的点 | 不做对的后果 |
| --- | --- | --- |
| 1 | **环形引用校验**（移动课题时） | 进度聚合无限递归，**直接拖垮服务** |
| 2 | **递归进度聚合** | 父课题进度与子树不一致 |
| 3 | **删除上提**（不级联） | 一次误删丢掉整棵子树的工作量记录 |
| 4 | **每个课题必须有负责人** | 中间层无人认领，正是本工具要解决的"谁负责"问题的反面 |

---

## 4. 页面清单（11 个）

| 页面 | 关键点 |
| --- | --- |
| **登录页** | 手机号 + 密码；注册入口；忘密码提示（引导联系会长重置） |
| **我的任务**（首页） | 按截止时间排序；逾期红色高亮；阻塞项单独一组；任务标注所属课题 |
| 任务列表 | 按部门 / 按人 / 按状态 / **按课题** 筛选；右上角新建任务 |
| 任务详情 | 改状态、填阻塞原因、加入日历 |
| **课题列表** | **树形展示**，可展开 / 折叠；每个节点显示 `已完成 n / 共 m`（含整棵子树）；支持新建子课题、移动课题 |
| **课题详情** | 面包屑（祖先链）+ 子课题列表 + 本级任务清单 + **递归聚合进度**；部长在此"跟进课题" |
| 成员名录 | 按 4 个组织分组；点进去看某人负责的事 |
| 加入流程 | **注册口令 + 手机号/姓名/密码** → 成为 `pending`，显示「等待管理员分配」页 |
| **待分配审批** | 待分配列表；支持**多选批量分配**部门与角色；显示来源（招募链接） |
| 管理 | 部门增改、分配 5 档角色、**注册口令与招募链接管理**、重置密码、**会长移交**；显示角色人数（软提示） |
| **我的 / 个人设置** | 本人信息、改密码、退出登录 |

> **v0.9 补 2 页**：登录页与「我的」。此前的 9 页清单里，`POST /auth/login`、`POST /auth/logout`、
> `PUT /auth/password`、`GET /auth/me` 的展示**都没有落点**。这不是细节——成员每次打开 App 都经过登录页，
> 而"退出登录在哪"是用户遇到问题时的第一反应。

---

## 5. 数据模型（6 张表）

### `Department` 部门 / 组织

| 字段 | 说明 |
| --- | --- |
| id | 主键 |
| name | 名称 |
| sort | 展示排序 |

> **v0.6 起删除原 `lead_id` 字段**：部长身份只由 `Member.role = lead` 表达。
> 同一事实存两份（部门表写一个、成员表写一个）必然会不一致，且没有权威答案。
> 需要展示部长时，查该部门 `role = lead` 的成员。

**v1 初始预设**（初始化时写入，可改名）：

| sort | name | 说明 |
| --- | --- | --- |
| 0 | 主席团 | 会长 / 副会长所属，权限覆盖全社团 |
| 1 | 课题部 | |
| 2 | 运营部 | |
| 3 | 宣传部 | |

### `Member` 成员

| 字段 | 说明 |
| --- | --- |
| id | 主键 |
| name | 姓名 |
| **phone** | **登录账号，全局唯一**；同时即联系方式（社团含校外人员，不用学号） |
| password_hash | 密码哈希，不落明文 |
| role | 固定 5 档枚举：`president` / `vice_president` / `lead` / `vice_lead` / `member`；`pending` 时为空 |
| dept_id | 所属组织（单层，4 选 1）；`pending` 时为空 |
| **dept_hint** | **招募链接带来的部门预填**（v0.11 新增）。只用于 `GET /members/pending` 展示来源，**不代表已被分配**；转 `active` 时由会长确认实际部门 |
| joined_at | 加入时间 |
| **status** | **`pending`（待分配）/ `active`（正式）/ `disabled`（已退出）** |

> **账号与权限生命周期独立**：换届只改 `role`，毕业回来只需重新赋角色，都不重建账号。

### `Plan` 课题

课题是**可嵌套的容器**：一个课题下可挂子课题，层层往下分割；**任务可挂在任意层级的课题上，任务本身不再分层**。

| 字段 | 说明 |
| --- | --- |
| id | 主键 |
| title | 课题名称 |
| desc | 目标 / 说明 |
| **parent_id** | **父课题（可空；空 = 顶层课题）**。自引用，由此支持任意层级 |
| **owner_id** | **负责人（必填——任意层级的课题都必须有）** |
| dept_id | 所属组织。**v0.8 起只在顶层课题上存**；子课题不存该字段，部门由所属根课题决定 |
| due_at | 可选截止时间 |
| created_by / created_at | 创建人与创建时间 |

> **`Plan` 故意不设 status 字段**：进度由**整棵子树**的任务递归聚合而来，理由见第 6 节规则 7。
>
> **v0.8：`dept_id` 只保留在顶层课题。** 初稿让每个节点都存、并规定"子课题继承父课题"，
> 但靠复制一份实现的"继承"**仍是同一事实存两份**——与已删除的 `Department.lead_id` 是同类问题。
> 去掉后：「子课题不允许跨部门」自动成立，移动课题也**不需要级联更新部门**。

### `Task` 任务

| 字段 | 说明 |
| --- | --- |
| id | 主键 |
| title | 标题 |
| desc | 描述 |
| **owner_id** | **负责人（必填，且仅一人）** |
| plan_id | 所属课题（**可选**；为空表示独立任务） |
| dept_id | 部门标签（可选，v1 先留字段） |
| status | 4 档：未开始 / 进行中 / 阻塞 / 已完成 |
| **blocker** | **阻塞原因（短文本）** |
| due_at | 截止时间 |
| **updated_at** | **最近更新时间（服务端生成，不接受客户端传入）** |
| completed_at | 完成时间（进入 `done` 时记录） |
| deleted_at | **软删除标记**；非空表示已删除，不出现在任何列表 |
| source | 来源：会议 / 临时（可选，为第二阶段留口子） |

> **v0.7 起删除原 `calendar_event_id`**：日历事件属于**个人设备**，且同一任务可被多人加入日历，
> 服务端存一个 id 无法表达，换手机后还会变成悬空引用。改由**客户端本地**维护映射，见第 7 节。
| created_by / created_at | 创建人与创建时间 |

> 加粗字段是第一版让「做到什么程度」可信的核心，也是给第二阶段会议纪要转任务预留的接口。
> v0.2 曾加过 `result` 字段，**v0.3 已依甲方答复④移除**。

### `RegisterConfig` 注册配置（全局单例）

| 字段 | 说明 |
| --- | --- |
| code | **社团共享注册口令（明文）**；会长可在管理页随时更换 |
| updated_by / updated_at | 最近修改人与时间 |

> v0.4 用它取代了原 `Invite` 一次性邀请码表：共享口令**不承载任何角色**，只决定"能否注册"。
> 换届时不需要重发，新成员问一句即可注册。
>
> **v0.6：口令存明文**（与个人密码一律存哈希不同）。它不是个人凭证，而是像群邀请码一样
> **需要被随时展示**给新成员的东西。若只存哈希，会长每次想知道当前口令就只能"重置"，
> 而重置会让正在注册的人失败。**仅会长 / 副会长可读。**

### `DeptInviteLink` 部门招募链接

| 字段 | 说明 |
| --- | --- |
| token | 链接标识（随机串） |
| dept_id | 定向组织 |
| enabled | 是否启用 |
| created_by / created_at | 创建人与时间 |

> ⚠️ 招募链接**只预填部门**，不赋予任何角色。通过它注册的人仍是 `pending`，**必须经会长确认**才转为 `active`。

---

## 6. 关键业务规则

1. **一个任务只能有一个负责人**（必填）。允许多个负责人等于没人负责，这正是本工具要解决的问题本身。
2. **状态只有 4 档，不做进度百分比**。百分比最终一定会永久停在 90%。
3. **阻塞原因**把「进度落后」翻译为「需要帮助」，是整个工具信息量最大的一个字段。
4. **最近更新时间自动记录**。一个任务三周无人动，这个事实本身就应被看见，比任何百分比都有意义。
5. **角色固定 5 档，只能分配、不能自定义**。权限矩阵见 3.4.3。
6. **人数只做软提示，不做硬校验**。换届、代理、临时增补在社团里是常态。
7. **课题不单独设状态，进度由整棵子树递归聚合**（显示为 `已完成 n / 共 m`，`n` / `m` **含所有后代任务**）。
   这样可以从结构上杜绝「任务全完成了、课题还写着进行中」这类不一致——它不需要靠人去维护。
8. **任务可以不属于任何课题**。运营部、宣传部的零散日常任务不必强行挂课题，避免为了用工具而造课题。
9. **删除课题时，其下子课题与任务一律「上提一级」**（挂到被删课题的父节点；被删的是顶层则变为顶层 / 独立），**绝不级联删除**。
   任务承载着人的工作量记录，不能因为删一个容器就消失。
10. **系统必须始终至少有一个 `president`。** 不允许把最后一个会长降级或删除——否则社团会失去全部管理能力，且**无法自助恢复**，只能改服务器数据文件。
11. **会长移交是一个明确操作**，不是"手动改两次角色"。分两步做会出现「零会长」的中间态，正好撞上第 10 条。
    > **v0.11 补充**：因此**会长只能由「移交会长」产生**。`assign` 与 `PATCH /members/{id}` 一律拒绝
    > `role = president`——否则会出现两个会长，第 10 条与第 11 条同时失去意义。
12. **注册与授权分离**：注册只创建账号（`pending`，无任何权限），部门与角色一律由会长授予。**换届、毕业、返校都只改角色，不重建账号。**
13. ⚠️ **课题树禁止环形引用。** 移动课题时，目标父节点**不能是该课题自身或其任何后代**——否则递归进度聚合会无限循环并拖垮服务。这是多级结构下最容易出的事故，**必须做校验**。
14. **任意层级的课题都必须有负责人。** 层层往下分割最大的风险是中间层无人认领，而那正是本工具要解决的「谁负责」问题的反面。
15. **「逾期」不是状态，是派生量。** 任务状态永远只有 4 档，`is_overdue` 由 `due_at` 与当前时间算出。
    与规则 7 同源：**能从事实推导的东西就不要单独存**，否则两边必然不一致。
16. **`updated_at` 必须由服务端生成**，不接受客户端传入。它是「做到什么程度」可信度的基础——一旦允许客户端写，这个字段就失去意义了。
17. **任务采用软删除**（`deleted_at`），保留工作量记录。与规则 9 同一条原则：不能因为删一个容器就让工作痕迹消失。
18. **课题树深度硬上限 6 层**（`PLAN_DEPTH_EXCEEDED`）。环形校验防的是环，防不了"上千层直线嵌套"这类病态数据——上限的意义是**防呆**，不是限制表达。
19. **`Plan.dept_id` 只存在顶层课题**，子课题的部门由所属根课题决定。「继承」若靠复制一份，就又变成同一事实存两份。

---

## 7. 日历同步设计

选择「加入系统日历」把提醒交付交给手机自带日历，因此 v1 **不需要**后台推送、通知权限与服务端推送，这是本版最大的技术风险规避。

**⚠️ v0.7 修正：这件事发生在客户端，不在服务端。**

初稿把 `calendar_event_id` 存在服务端的 `Task` 上，**这个位置是错的**：

1. 日历事件在**成员自己的手机上**
2. **同一任务可能被多人加入日历**（负责人、部长、关注者），服务端存一个 id 无法表达
3. 换手机后本地事件消失，服务端那个 id 成为**悬空引用**
4. 服务端**不应该也不需要**知道谁把什么事加进了自己的日历——这本身是隐私

**正确划分：**

| 位置 | 存什么 |
| --- | --- |
| **客户端本地** | `task_id → { event_id, cached_due_at, cached_status }` |
| **服务端** | 只提供事实：`due_at`、`status`、`updated_at`。**不存任何日历字段** |

客户端拉取任务时比对本地缓存与服务器值：

| 差异 | 客户端动作 |
| --- | --- |
| `due_at` 变了 | **更新**本地日历事件（不得新建） |
| `status` 变为 `done` | **删除**本地日历事件 |
| 任务已不在服务器返回中 | **删除**本地日历事件 |
| 日历权限被拒绝 | 降级为导出 `.ics` + 应用内高亮 |

服务端为此提供一个很小的辅助接口 `POST /tasks/lookup`（见 `api-design.md` §4.8），
让客户端知道"我加过日历的任务现在还在不在"。**没有它，客户端无法可靠回收日历事件。**

> 不做「更新与删除」这一步，日历里会堆积错误的过期提醒，两周后成员就不再相信它。
> **工作量没有消失，只是换到了客户端**——而且换过去**更好验证**，因为日历同步错了是用户看得见的。
> **需在客户端排期里单独列出这一项。**

---

## 8. 技术前提（缺一项 v1 跑不起来）

1. **服务端 + 账号体系（必须）**。「谁负责」天生是跨人共享的数据，纯本地 App 无法实现。此条不在功能清单内，但是地基。
2. **后端技术栈**：仓颉 **1.1.3** + 轻舟框架，部署在云服务器（**Windows**，与开发机同平台）。服务端与客户端的编译链版本必须对齐。
3. **部署方式：本机编译 + 拷贝制品**。因为服务器与开发机**同平台**，本机编好的 exe 可直接复用，**服务器上不需要安装编译器**。打包内容为 **exe + 4 个 DLL**（共约 18.8 MB）：`club-server.exe`、`libcangjie-runtime.dll`、`libboundscheck.dll`、`libcrypto-3-x64.dll`、`libssl-3-x64.dll`（由 `server\build-package.ps1` 产出）。详见 `deploy-windows-verify.md`。
4. **持久化：文件存储**（内存 Store + 写时原子落盘 JSON）。仓颉标准库 / stdx / 轻舟均无关系型数据库驱动，自建 `sqlite3` FFI 绑定不在 v1 范围内。**数据访问必须抽成 Store 接口**，以便日后替换实现。
5. **OpenSSL 3 必须随制品一起部署**：stdx 的 crypto / tls 硬编码 `dlopen` OpenSSL **3**。**`libcrypto-3-x64.dll` / `libssl-3-x64.dll` 必须与 exe 同目录**，缺失时密码哈希与 session 会**运行时 500 且编译期无警告**（与开发机上的现象一致）。
6. **ABI 配置**：客户端已于 2026-09-14 改为 **ArkTS**，原先 `entry/build-profile.json5` 里的 `cangjieOptions.abiFilters` 已删除（`cangjieOptions` 会被 stock hvigor 直接拒绝，见 `client-build.md`）；ABI 现由 DevEco 的 ArkTS 构建默认处理。
7. **分发渠道（非技术卡点，需尽早并行启动）**：HarmonyOS NEXT 侧载受限，把 App 发到几十位成员手上通常需走 AppGallery Connect 内部测试轨道，涉及签名证书与测试版本审核。**该流程耗时可能超过开发本身，不要等开发完成才启动。**

---

## 9. 阶段规划

### 第二阶段

1. **会议**（优先）：会议纪要是任务最自然的来源，与 v1 衔接最紧。
2. **新人必读**：只做「一篇置顶文档 + 附件」的最小形态，不做全文检索与权限。针对当前大量招募的场景，这是即时痛点。
3. **成果附件上传**：若甲方日后提出需要提交物，再补（v1 已确认不需要）。

### 第三阶段

- 完整知识库（内容维护成本与任务工具完全不同，需先明确「谁来写」）

> 原「项目容器」条目已移入 v1（甲方答复③），此处不再重复。

---

## 10. 风险与待定事项

| 事项 | 状态 |
| --- | --- |
| 服务器架构是否确为 **x64**（若是 ARM64 需重编） | **先跑步骤 0**，见 `deploy-windows-verify.md` |
| 防火墙 + 云安全组**两层**都要放行 | 常见部署卡点，同上 |
| 课题容器带来的约 +20~25% 工作量 | 已计入，需确认工期可接受 |
| **招新期会长审批成为瓶颈** | **需缓解**：部门招募链接 + 批量分配，见第 2 节 v0.4 修订说明 |
| **课题树环形引用导致递归失控** | **最高优先**：移动课题时必须校验，见第 6 节规则 13 |
| 多级课题使 UI 复杂度上升 | 已计入 +15% 工作量，见 3.5.2 |
| 云服务器可用期限与归属（老师提供） | **需确认**，关系数据能否长期留存 |
| 服务器能否访问外网、端口与安全组限制 | 待确认 |
| 备份落地位置与交接人 | **需指定至少两人** |
| 成员使用的鸿蒙设备版本分布 | 待确认 |
| 分发渠道启动时间 | **建议立即启动** |
| 日历 API 的具体权限与容量限制 | 开发前验证 |
| 试用期起止日期 | 待定 |

---

## 附：范围变更规则

任何新增功能进入 v1，必须同时满足：

1. 直接服务于第 1 节的两个问题之一；
2. 不引入新的权限模型或新的外部依赖；
3. 试用验收标准（第 2 节）不被推迟。

否则一律记入第二阶段候选，不进入 v1。

> **本次变更留痕**：③「课题容器」按本规则评估后准予进入 v1，评估表见 3.5；④⑤ 为范围缩减，无需评估。
