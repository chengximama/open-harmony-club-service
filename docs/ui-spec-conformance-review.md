# UI 设计规格 ↔ 服务端 一致性审计

> 审计对象：`鸿蒙俱乐部-全场景UI设计规格.pdf`（13 页，M1–M8）与 `server/` 下全部源码。
> 范围：**只审服务端**（客户端由组内其他成员负责，本文不评价客户端实现）。
> 审计时间：2026-09-16 ｜ 工作区：`E:\harmonyOS\cangjie_web`

---

## 0. 一句话结论

**以已冻结的 `api-design.md` / `v1-scope.md` 为尺子，服务端是达标的**：40 / 40 接口全部注册，四套测试本次全部实跑复现通过。

**以这份 UI 设计规格 PDF 为尺子，则不然**：存在 **5 处功能性缺口（高）**、**5 处口径冲突（中）**、**5 处次要差异（低）**，另有 **4 处是设计稿自身矛盾**（应改稿而非改代码）。其中 **D-15 是队友独立复验时抓到的，我第一轮审 P01 时漏了**（§0.5 已修）。

PDF 里直接印出了本项目的字段名与接口名（`dept_hint`、`error.fields`、`GET /auth/me`、`view_scope`、`assign-batch`、`owner_id`、`DEPT_NOT_EMPTY`、`FORBIDDEN_ROLE`），说明它是与本项目**同源**的 UI 规格，因此下列差异不是"客户端的事"，而是**服务端契约要不要跟着改**的问题。

---

## 0.4 队友复验 8 条的逐条对账（2026-09-16）

> 队友的复验是对 `F:/object/open-harmony-club-service`（他的克隆）做的，那份代码是**今天改动之前**的状态。
> 下表按**当前工作区**逐条核实。

| 队友条目 | 结论 | 依据 |
| --- | --- | --- |
| 1. P0 首页阻塞卡拿不到阻塞原因 | **属实，本轮已修**（D-15） | `blocker` 原先只在 `taskDetail`；现已上移到 `taskBrief` |
| 2. P1「部长在首页看到求助」没实现 | **已解决**（D-4） | `groupMine` 增加 `deptScopeId`：部长/副部长额外带出本部门 `blocked`；`counts.borrowed_blocked`；求助对象字段 `needs_help` |
| 3. P1 没有「逾期 N 天」 | **已解决**（D-5） | `TaskBrief.overdue_days`（本地日界差）。**未加 `server_now`**：分组与天数都由服务端算好，客户端不需要本地时钟参与判定 |
| 4. P1 注册密码规则冲突 | **✅ 已定稿并落地**（D-6） | 定为 **8–32 字节 + 只允许数字/英文/符号**（上限 32 采纳 P12、下限取 8 有意偏离 P12 的 6、字符集为本次新增）；`passwordError()` 作唯一入口，注册/改密/`init-admin` 三处共用 |
| 5. P1 任务列表 `size=500` 与上限 200 冲突 | **不成立** | 规格 P02 原文是 **`分页 size=50。`**（2× 放大原图核对），服务端默认正好 50；`size=500` 是客户端才会传的值，规格没这么要求 |
| 6. P2 成员搜索没有服务端支持 | **已解决**（D-2） | `GET /members?q=`：姓名 OR 部门名；服务端过滤后才分页，不受 200 上限影响 |
| 7. P2 成员详情统计字段不足 | **已解决**（D-7） | `stats` 增 `done_tasks` / `overdue_tasks` |
| 8. P2 招募链接→App 部门预填未闭环 | **✅ 本轮已闭环**（D-16） | 新增公开 `GET /api/v1/join/{token}` → `{dept, enabled}`；注册可带 `invite_token`，**部门由服务端从 token 推导**（优先级 token > `dept_id`），响应回显 `dept_hint` 使生效与否可观察 |

> 队友写"其余核心能力均已有实现"的那批（课题树递归进度、六层限制、删除上提、同权保护、批量分配逐条结果、会长原子移交、注册限流、任务软删除、服务端首页分组）与本文 §6「已对齐」清单一致。

---

## 0.5 处置结果（2026-09-16 定稿）

产品/开发决定：**D-1 / D-2 / D-3 / D-4 / D-5 / D-7 按设计规格改服务端；D-15（队友复验补的 P0）同属设计规格要求，一并修；其余保留原版本。**

| # | 事项 | 决定 | 落地情况 |
| --- | --- | --- | --- |
| **D-1** | 权限摘要补 5 个管理布尔 | ✅ 按设计稿 | `views.cj` `permissionsOf` 增补 `manage_depts` / `view_register_code` / `change_register_code` / `manage_invite_links` / `transfer_presidency`，**全部由 `can()` 推导** |
| **D-2** | 名录按姓名或部门搜索 | ✅ 按设计稿 | `GET /members?q=`（包含匹配、大小写不敏感；**手机号不参与**；待分配按 `dept_hint` 命中） |
| **D-3** | 任务/课题可见范围 | ✅ 按设计稿（**只放开读**） | `perms.cj` `scopeAllows` 放行 `ViewTasks`；`view_scope` 对 active 统一 `all`。**写范围一字未改**，并在冒烟里加了"改外部门任务状态仍 403"的反例 |
| **D-4** | 需要帮助 + 部长首页 | ✅ 按设计稿（**C 方案：两者都要**） | `Task` 增 `help_dept_id` / `help_member_id`；`PUT /tasks/{id}/status` 可填，离开 blocked 自动清空；视图给 `needs_help`；`/tasks/mine` 对部长/副部长带出本部门 blocked（`counts.borrowed_blocked`） |
| **D-5** | 逾期天数 | ✅ 按设计稿 | `TaskBrief.overdue_days`（本地日界差；未逾期为 0） |
| **D-7** | 成员详情计数 | ✅ 按设计稿 | `stats` 增 `done_tasks` / `overdue_tasks` |
| **D-15** | **首页阻塞卡拿不到阻塞原因**（队友复验 P0，本轮补） | ✅ 按设计稿 | `blocker` 由 `TaskDetail` **上移到 `TaskBrief`**（`views.cj`）—— P01 的「阻塞中」卡片直接渲染阻塞原因高亮块，原先必须为每张卡再打一次详情接口。非阻塞任务给 `null` |
| **D-16** | **招募链接 → App 部门预填未闭环**（队友复验 P2，本轮补；**1+2 方案**） | ✅ 已闭环 | ① 新增公开 `GET /api/v1/join/{token}` → `{dept:{id,name}\|null, enabled}`（与落地页同语义，失效同形状）；② `POST /auth/register` 接受 `invite_token`，**部门由服务端解析**，优先级 `invite_token` > `dept_id`；响应回显 `dept_hint`。接口总数 **39 → 40** |
| **D-6** | 密码口径（长度 + 字符集） | ✅ **已定稿：8–32 字节 + 只允许数字/英文/符号** | `h_auth.cj` 新增 `passwordError()` 作唯一入口（长度 + 字符集），注册 / 改密 / `init-admin` 三处共用；上限 32 采纳 P12，下限取 8（有意偏离 P12 的 6），字符集为本次新增。中文与全角密码自此被拒 |
| D-8 | 待分配列表排序方向 | ⏸ 保留原版本 | 仍是"先来先处理"（按注册时间正序） |
| D-9 | 名录待分配组的「来源」 | ⏸ 保留原版本 | `dept_hint` 仍只在 `GET /members/pending` 返回 |
| D-10 | 名录分页 vs 全量分组 | ⏸ 保留原版本 | 分页仍是 50 / 上限 200 |
| D-11 | 招募链接 token 形态 | ⏸ 保留原版本 | 仍是 32 位十六进制（**实现更安全**，建议改设计稿示例） |
| D-12 | 「一次性」临时密码 | ⏸ 保留原版本 | 仍只在响应里回一次，不强制首登改密 |
| D-13 | 忘记密码引导角色 | ⏸ 保留原版本 | 仍允许部长 / 副部长重置低档位成员 |
| D-14 | 会长移交二次确认 | ⏸ 保留原版本 | 仍由客户端做确认 |
| E-1 ~ E-4 | 设计稿自相矛盾 | — | 仍待设计侧修订 |

**验证**：改完四套测试全绿 —— **单测 466 / 冒烟 421 / TLS 22 / 契约 30**（那一轮的基线；随后一轮读码修复又补了 5 条闸门，单测为 **471**）
（改动前 398 / 362 / 22 / 30；新增"回退即变红"闸门：单测 +58 条、冒烟 +50 条）。

**文档同步**：`api-design.md`（§2.3 注册字段与优先级、§2.6 权限表、§2.7、§3.2、§3.7 公开解析端点、
§4.1、§4.2、§4.6、§4.9、§6.1 清单 39→40、§6.4）·
`v1-scope.md`（§3.4.3 权限矩阵 + v0.12 修订记录）· `frontend-brief.md`（§4、§5、§6②）·
`README.md`（进度表 + 四套测试 + 接口口径）· `HANDOFF.md`（基线 + 文档地图 + M12）。

> ⚠️ **D-3 是本次唯一的权限语义变更**，动的是 `perms.cj`（全项目唯一的权限判定点）。
> 边界写死为：**读全社团、写不变**。今后若有人把写也放开，冒烟里的
> 「部长改外部门任务状态 -> 403（D-3 只放开读）」会立刻变红。

---

## 1. 验证基线（复现命令 / 实跑结果）

> 下表是**改动前**的基线（2026-09-16 审计当时）。改动后的基线见 §0.5：466 / 421 / 22 / 30（后续读码修复后为 471 / 421 / 22 / 30）。

在 `E:\harmonyOS\cangjie_web\server` 下：

```powershell
.\build\club-server.exe test                                        # 单测
powershell -NoProfile -ExecutionPolicy Bypass -File .\tests\smoke.ps1
powershell -NoProfile -ExecutionPolicy Bypass -File .\tests\tls-check.ps1
powershell -NoProfile -ExecutionPolicy Bypass -File .\tests\client-contract-check.ps1
```

| 验证 | 结果 |
| --- | --- |
| 单测 | **PASS 398 / FAIL 0** |
| HTTP 冒烟 | **PASS 362 / FAIL 0** |
| TLS 关卡 | **PASS 22 / FAIL 0** |
| 前后端契约（客户端声明的 30 条调用逐条打真实服务端） | **PASS 30 / FAIL 0** |

接口注册数逐条核对（`server/src/main.cj`）：认证 5 + 组织与成员 20 + 任务 8 + 课题 6 + 运维 1 = **40 / 40**，与 `api-design.md §6.1` 一致；另有清单外的公开落地页 `GET /join/{token}` 与运维端点 `POST /admin/shutdown`。

### 1.1 设计规格 13 页 → 里程碑 → 服务端触点

| 页 | 屏幕 | 里程碑 | 主要服务端触点 |
| --- | --- | --- | --- |
| 01 | 我的任务（首页） | M5 | `GET /tasks/mine`、`PUT /tasks/{id}/status` |
| 02 | 任务列表 | M5 | `GET /tasks` |
| 03 | 任务详情 | M5 | `GET /tasks/{id}`、`PUT /tasks/{id}/status`、`DELETE /tasks/{id}`、`POST /tasks/lookup` |
| 04 | 课题树 | M6 | `GET /plans` |
| 05 | 课题详情 | M6 | `GET /plans/{id}`、`POST /plans`、`POST /plans/{id}/move`、`DELETE /plans/{id}` |
| 06 | 成员名录 | M3 | `GET /members`、`GET /depts` |
| 07 | 成员详情 | M3 | `GET /members/{id}`、`GET /tasks?owner_id=`、`PATCH /members/{id}`、`POST /members/{id}/reset-password`、`POST /members/{id}/disable` |
| 08 | 待分配审批 | M2 · M3 | `GET /members/pending`、`POST /members/{id}/assign`、`POST /members/assign-batch` |
| 09 | 管理 | M2 | 部门 CRUD、`GET/PUT/POST /register-config*`、`GET/POST/DELETE /dept-invite-links`、`POST /members/{id}/transfer-presidency` |
| 10 | 我的 · 个人设置 | M8 | `GET /auth/me`、`PUT /auth/password`、`POST /auth/logout` |
| 11 | 登录页 | M1 | `POST /auth/login` |
| 12 | 加入社团（注册） | M1 | `POST /auth/register`、`GET /join/{token}` |
| 13 | 等待管理员分配 | M1 | `GET /auth/me` |

---

## 2. 高优先级（建议合并前处理）

### D-15 首页阻塞卡拿不到阻塞原因（队友复验 P0；**我第一轮漏审**）

| 项 | 内容 |
| --- | --- |
| 设计稿 | P01「阻塞中」分组卡片内直接渲染**阻塞原因高亮块**：`阻塞原因（进入 blocked 必填）` + 原因原文（示例：`等宣传部出主视觉，主视觉未定则排版无法开始。`） |
| 服务端现状（改动前） | `blocker` **只在 `TaskDetail` 里**（`views.cj` 的 `taskDetail`）。`/tasks/mine` 返回的是 `TaskBrief`，**没有这个字段** |
| 后果 | 客户端要让首页那张卡显示原因，就得**为每张卡再打一次 `GET /tasks/{id}`**；而 P01 的原话正是「服务端已排好序，客户端只渲染、不重算」——首页本该一次拿全。N 张阻塞卡 = N+1 个请求 |
| 修法（本轮已做） | `blocker` 从 `TaskDetail` **上移到 `TaskBrief`**：`/tasks`、`/tasks/mine`、`/plans/{id}` 的任务列表都直接带原因；非阻塞任务给 `null`（不是空串） |
| 为什么我漏了 | 我按"字段→接口"逐条对照时，把 `blocker` 归到了"详情字段"，没有回到 P01 的**卡片截图**上确认它出现在列表里。教训：字段清单不能替代界面截图 |
| 归属 | **服务端改**（已完成） |

### D-1 权限摘要字段不足 → 「管理」页无法「按权限显示」

| 项 | 内容 |
| --- | --- |
| 设计稿 | P10：`个人信息 + 权限摘要（服务端下发）+ 按权限显示的管理入口 + 设置项 + 退出登录`；P09：四张分区卡「部门与组织 / 注册口令 / 招募链接 / 会长移交」 |
| 服务端现状 | `permissionsOf()`（`server/src/views.cj:58`）只下发 6 个字段：`view_scope` / `manage_members` / `set_role` / `create_plan` / `create_task` / `update_any_task` |
| 缺口 | 恰好**决定四张卡能否显示**的布尔一个都没有：部门增删改（会长独占）、注册口令可读（会长 + 副会长）/ 可写（会长）、招募链接管理（会长 / 副会长）、会长移交（会长） |
| 后果 | 副会长 `manage_members = true`，会看到「部门与组织」入口，点下去被 403 —— 正是设计稿要避免的场景 |
| 建议 | `permissionsOf` 增补 `manage_depts` / `view_register_code` / `change_register_code` / `manage_invite_links` / `transfer_presidency`，**全部由 `can()` 推导**（保持"权限判定只有一处"的铁律） |
| 归属 | **服务端改**（不改产品口径） |

### D-2 成员名录没有「搜索姓名或部门」

| 项 | 内容 |
| --- | --- |
| 设计稿 | P06 屏幕搜索框占位符原文：`搜索姓名或部门`；页眉注释：`搜索框 + 按 4 个组织分组的 sectioned 列表` |
| 服务端现状 | `GET /members`（`server/src/h_member.cj:60`）只支持 `dept_id` / `role` / `status` / `page` / `size`（`:63-65`），**无关键词参数** |
| 佐证 | 客户端 `entry/src/main/ets/api/MemberApi.ets:108` 也只拼 `dept_id` / `status`；`MemberListPage.ets:144` 是一个写死的 `🔍 搜索成员`，没有搜索逻辑 |
| 后果 | 分页默认 50、上限 200（`server/src/paging.cj:19,44`），而容量目标是"近千人"——客户端本地过滤不成立 |
| 建议 | 加 `q` 参数（姓名 OR 部门名模糊匹配，需支持中文）；或明确降级为"仅按部门筛选"并改稿 |
| 归属 | **服务端改**（需先确认搜索语义：前缀 / 包含 / 是否含姓名拼音） |

### D-3 任务 / 课题的可见范围冲突 —— ⚠️ 需产品决策

| 项 | 内容 |
| --- | --- |
| 设计稿 | P02 屏幕：`共 38 项 · 全社团范围可见`，卡片是 **宣传部 / 课题部** 的任务；P04 课题树用 `全部` 筛出 **主席团** 的课题；P03 阻塞块：`（部长在首页即可看到这条）`。三页合起来明确是**全社团可读** |
| 服务端现状 | `perms.cj:167` `scopeAllows` 的收尾判定是 `return t.dept_id == m.dept_id`（`perms.cj:204`），即部长 / 副部长 / 成员**只能看本部门** |
| 冻结文档 | `v1-scope.md:125`「查看任务 / 课题：会长全社团、部长及以下本部门」；`:150`「任务与课题仍按部门限制，两者可见性不必一致」。`api-design.md §4.2` 同 |
| 性质 | **两份设计文档打架**，不是代码 bug |
| 建议 | 二选一并同步文档：① 以 PDF 为准 → 放开 `ViewTasks` 范围（有跨部门可见的安全影响，需重跑权限单测与冒烟）；② 以冻结文档为准 → 改 PDF 的 P02/P04 文案 |
| 归属 | **需决策** |

### D-4 「需要帮助」求助标记 + 部门部长首页可见

| 项 | 内容 |
| --- | --- |
| 设计稿 | P03 阻塞块内一行原文：`需要帮助 · 宣传部 陈屿（部长在首页即可看到这条）` |
| 服务端现状 | ① 无 `needs_help` / 求助对象字段（全仓 grep 零命中，`docs/` 亦无）；② `groupMine`（`server/src/h_task.cj:57`）只取 `t.owner_id == 自己` 的任务（`:71`），**没有**"本部门被求助的阻塞项进入部长首页"这条可见性规则 |
| 性质 | PDF 独有的**新需求**，冻结文档里没有对应条款 |
| 建议 | 先确认是否属于 v1。若做，涉及「字段 + 首页聚合口径 + 跨人可见性」三件事，且与 D-3 同源 |
| 归属 | **需决策** |

---

## 3. 中优先级

### D-5 逾期天数未下发

- 设计稿：P01 卡片 `逾期 2 天`；P03 提示条 `已逾期 2 天 · 该判断由服务端算出，客户端不重算`，视觉块再次强调 `逾期提示条说明「由服务端算出」`。
- 服务端：`TaskBrief` 只有布尔 `is_overdue`（`server/src/views.cj:301`），**没有天数**。
- 后果：客户端要么自己算（正是设计稿禁止的），要么另开接口。
- 建议：`TaskBrief` 增加 `overdue_days`（按服务端本地日界计算，与 `groupMine` 的 `todayStart` 同源）。
- 归属：**服务端改**。

### D-6 密码长度三方不一致 → **已定稿：8–32 字节 + 只允许数字/英文/符号**

- 设计稿 P12，密码框下方原文（本人在原图上核对过）：`长度按字节计：6-32 位，中文一字 3 字节。`
- 服务端（改动前）：注册 `h_auth.cj` 是 `pw.size >= 8`，改密同一句；**两处都没有上限**。
- 冻结文档：`api-design.md §2.3 注 5` 写「≥ 8 位」。
- **2026-09-16 定稿**（开发者决定）：

  | 维度 | 规则 | 与设计稿的关系 |
  | --- | --- | --- |
  | 长度 | **8–32 字节** | 上限 32 **采纳** P12；下限取 **8 而非 6**（6 位空间太小，且沿用服务端既有强度口径）→ 有意偏离 |
  | 字符集 | **数字 / 英文 / 符号** | P12 未提；本次新增（判定 = 可见 ASCII `0x21`–`0x7E`） |

- 实现：`h_auth.cj` 的 `passwordError()` 是**唯一入口**（先长度、后字符集），注册 / 本人改密 /
  `init-admin` 三处共用。加了字符集之后**中文、全角、空格/TAB 一律被拒**，
  P12 那句「中文一字 3 字节」只影响长度怎么算。
- 连带影响：文档里所有中文密码示例（`你的密码123` / `你的口令`）**已全部换成 ASCII**
  （README / HANDOFF / local-deploy / server-guide / client-integration-review\*），
  否则照文档敲的 `init-admin` 会被新规则拒绝。
- 归属：**已落地**（服务端改 + 文档同步）。

### D-7 成员详情身份卡缺两个计数

- 设计稿 P07 身份卡：`2025-09 加入 · 未完成 5 · 逾期 1 · 已完成 12`。
- 服务端：`memberDetailView`（`server/src/views.cj:171`）只给 `member` + `stats: { owned_tasks, open_tasks, owned_plans }`（`:191-195`）。
- 缺口：`已完成` 可由 `owned_tasks - open_tasks` 推出，但 **`逾期` 推不出来**，客户端还得再打一次 `GET /tasks?owner_id=X&overdue=true` 读 `total`。
- 建议：`stats` 增加 `overdue_tasks` / `done_tasks`（服务端一次遍历即可，成本为零）。
- 归属：**服务端改**。

### D-8 待分配列表排序方向与设计稿相反

- 设计稿：P08 列表顺序（本人在原图上核对过）`今天注册 → 昨天注册 → 2 天前注册`（**最新在前**）；P06 的「待分配 · 3」组同样以 `今天注册` 打头。
- 服务端：`pendingMembers()`（`server/src/store.cj:855`）按 `m.id` 升序 = **最早注册在前**（`:863`）；`api-design.md §3.3` 写「按注册时间正序（先来先处理）」。
- 建议：二选一。若按设计稿，改动只有一行（排序键取负）。
- 归属：**需决策**（一行改动）。

### D-9 名录「待分配」组的「来源」普通成员拿不到

- 设计稿 P06：`待分配单独成组并标出来源`，示例 `今天注册 · 来自招募链接（运营部预填）`。
- 服务端：带 `dept_hint` 的只有 `GET /members/pending`（`server/src/views.cj:200` `pendingView`），而该接口要求 `AssignPending` 权限（`server/src/h_member.cj:113`，仅会长 / 副会长）。
- 而 `GET /members?status=pending` 走的是 `memberBrief`（`server/src/h_member.cj:85`），**不含 `dept_hint`**；名录本身又是「全社团可见」的页面。
- 建议：要么让 `memberBrief` 对 `pending` 成员带 `dept_hint`，要么接受"普通成员看不到来源"并改稿。
- 归属：**需决策**（涉及最小化暴露面）。

---

## 4. 低优先级

| # | 设计稿 | 服务端现状 | 建议 |
| --- | --- | --- | --- |
| **D-10** | P06 名录一次展示 42 位并给出 `42 位成员 · 4 个组织 · 待分配 3 人` 三处计数 | `GET /members` 分页默认 50、上限 200（`paging.cj:19,44`），返回扁平列表 + `{items,total,page,size}` | 千人规模下客户端需循环拉取；可考虑服务端给分组接口或提高上限 |
| **D-11** | P09 示例链接 `/join/9f3c-a21b`（9 字符短码） | `freshLinkToken` 生成 **32 位十六进制**（`server/src/h_link.cj:100`，`randHex(16)`） | 属**有意偏离**（`code-review.md` N-19 为防枚举主动加长），实现更安全 → **改稿**，不要改代码 |
| **D-12** | P07 `重置密码 · 签发一次性临时密码` | `h_secret.cj:89` 只在响应里回一次，该口令之后**长期有效**，没有"首次登录必须改"的强制 | 若要"真一次性"，需加"首登强制改密"标志；v1 可先在文档里写明语义 |
| **D-13** | P11 `忘记密码？请联系会长或副会长在管理页为你重置` | `api-design.md §3.5`：会长 / 副会长 / **部长 / 副部长**（限本部门、档位更低）都可重置 | 服务端是**超集**，不影响功能；建议在文档里对齐措辞 |
| **D-14** | P09 `会长移交 … 需要二次确认` | `POST /members/{id}/transfer-presidency`（`server/src/h_member.cj:430`）无 confirm 参数，只有 `self_role` | 若二次确认归客户端（现有实现）可不动；若要求服务端强制，需加一次性确认令牌 |

---

## 5. 设计稿自身矛盾（改稿，不动代码）

| # | 矛盾 | 说明 |
| --- | --- | --- |
| **E-1** | P05 卡片 `已完成 4 / 共 7 · 3 个子课题 · 本级任务 3` vs 同页列表标题 `子课题 · 2` | 且 `4 / 7` 恰好只等于两个子课题之和（`2/3` + `2/4`），**未含 `本级任务 3`**，与同页注释 `含整棵子树` 字面冲突。服务端口径（`server/src/store.cj:1301` `planStatsAll`）= **本级 + 整棵子树**，与注释一致 → 是稿子的数字要修 |
| **E-2** | P10 权限示例 `view_scope = all · 全社团` **且** `manage_members = false` | 服务端模型里两者同源于一个 `global` 标志（`views.cj:61-70`），**不可能同时出现**，示例值是占位数据 |
| **E-3** | P06 `42 位成员 · 4 个组织 · 待分配 3 人`，但可见分组是 `主席团·4` + `课题部·13` + `待分配·3 = 20 ≠ 42`；P04 的部门胶囊只列 3 个部门 | 需澄清是"示意省略其余分组"还是数据错；分组人数之和应等于 `total` |
| **E-4** | P12 密码字段下的 `长度按字节计：6-32 位` | 数字与 `api-design.md §3.6` 的**注册口令**规则（6–32 位）完全一致，疑似串行；见 D-6 |

---

## 6. 已对齐的部分（无需改动）

以下设计与服务端**逐条对得上**，列出来是为了让这份报告可被反证：

**任务（P01 / P02 / P03）**
- `/tasks/mine` 由服务端分组（`overdue` / `blocked` / `due_today` / `due_week` / `later` / `no_due`）+ `counts: {overdue, blocked, open_total}`，组内按 `due_at` 升序 —— 对应 P01 原话 `服务端已排好序，客户端只渲染、不重算。`（`server/src/h_task.cj:115`、`store.cj:1058`）
- **阻塞项与时间组双套并行**：`blocked` 是额外一组，不互斥 —— 对应 P01 `阻塞项同时也会出现在下面的时间分组里`
- 进入 `blocked` 必填原因 → `BLOCKER_REQUIRED`（`h_task.cj:512`）；离开自动清空
- 状态只有 4 档、允许任意互跳、进 `done` 记 `completed_at`（`h_task.cj:494`）
- 删除是**软删除**（`h_task.cj:543`）
- `POST /tasks/lookup` 供日历同步回收（`h_task.cj:571`），服务端不存任何日历字段
- `TaskBrief` / `TaskDetail` 字段齐备（`views.cj:278`、`:308`），含 `is_overdue` / `plan_path` 面包屑

**课题（P04 / P05）**
- `GET /plans` 一次拉取整棵树、不分页、`depth` 默认 6（`h_plan.cj:37`）—— 对应 P04 `整棵树一次拉取`
- 递归聚合进度 `progress: {done,total}`（含本级 + 整棵子树）+ 直接子课题数 `child_count` —— 对应 P04 `进度是含整棵子树的递归聚合值`
- **课题无状态字段**（`Plan` 模型里就没有 status）—— 对应 P04 `课题本身没有状态字段`
- 删除**子节点上提一级、绝不级联**（`h_plan.cj:468`、`store.cj:1405`）—— 对应 P04 `删除父级时子节点自动上提一级`
- 深度上限 6（创建 + 移动都校验，`h_plan.cj:199`、`:408`）—— 对应 P05 `超过 6 层直接禁用`
- 环形引用校验（`planWouldCycle`，`h_plan.cj:394`）—— 对应 P05 `自身与全部后代替灰不可选`
- 子课题部门继承自根、拒绝单独指定（`h_plan.cj:191`、`:265`）—— 对应 P05 `子课题继承根部门，不可单独指定`
- 详情给面包屑 `path` + 子课题 `children` + **本级**任务 `tasks` 三段分离（`h_plan.cj:100-125`）—— 对应 P05 `子课题` / `本级任务` 两个独立集合

**成员（P06 / P07 / P08）**
- **名录不含手机号**：`memberBrief` 无 `phone`（`views.cj:18`），手机号只在 `memberSelf`（本人视图，`views.cj:38`）—— 对应 P06 `名录不含手机号——这是隐私最小化`
- 名录**全社团可见**（`scopeAllows` 对 `ViewMembers` 直接 `return true`，`perms.cj:170`）—— 对应 P06 `共 42 位 · 全社团可见`
- 任务可按 `owner_id` 反查（`h_task.cj:175`）—— 对应 P07 注释 `（用 owner_id 反查）`
- 移出社团时校验名下未完成任务，清单落在 `error.fields.tasks`（`h_member.cj:30-42`、`:261`）—— 对应 P07 原文
- **同权保护**：`can` 的动作语义层统一拦 `ResetPassword / SetRole / DisableMember / EditMemberName / AssignPending`（`perms.cj:250-267`），会长也不能重置自己 —— 对应 P07 `不能对同权限或更高权限的人执行上述操作，会长也不能重置自己` 与 P08 的 `FORBIDDEN_ROLE`
- 角色枚举不含 `president`（`assign` / `PATCH /members` 一律拒绝，`h_member.cj:185`、`:287`）—— 对应 P07 `角色里没有「会长」`
- `assign-batch` **逐条报告、绝不整体回滚**（`h_member.cj:336`），失败条目带 `{id, code}` —— 对应 P08 `成功 2 条，失败 1 条（FORBIDDEN_ROLE…失败的条目保留选中以便重试）`
- `dept_hint` 只是提示、不授予任何角色（`h_auth.cj:275`、`h_member.cj:315`）—— 对应 P08 `dept_hint 只是招募链接预填的部门…口令与链接不承载任何角色`
- `dept_hint` 可为空（`pendingView` 给 `null`，`views.cj:202`）—— 对应 P08 `未指定部门`

**管理（P09 / P10）**
- 部门增删改**会长独占**（`perms.cj:121-122`）；删除非空部门 → `DEPT_NOT_EMPTY`，且额外挡住"还有顶层课题"（`h_dept.cj:160-167`）—— 对应 P09 `增删改仅会长；删除非空部门会被拒绝（DEPT_NOT_EMPTY）`
- 部门有 `sort` 字段与范围校验（`h_dept.cj:49`，`SORT_MIN/SORT_MAX`）—— 对应 P09 部门行的 `排序`
- 注册口令**明文**、仅会长 / 副会长可读（`ViewRegisterCode`，`perms.cj` 未对副会长排除）；**更换 / 轮换仅会长**（`ChangeRegisterCode`，`perms.cj:117`）；轮换后旧口令立即失效（`h_secret.cj:129`）—— 对应 P09 `口令明文展示但限定仅会长副会长可读`、`轮换后旧口令立即失效`
- 口令长度 6–32（`h_secret.cj:122-123`）—— 对应 `api-design.md §3.6`
- 招募链接可启停（`enabled`，幂等停用，`h_link.cj:176`）—— 对应 P09 `启用中` / `停用`
- 会长移交是**单事务原子操作**（目标升会长 + 自己降 `self_role`，一次落盘，`h_member.cj:456-459`）；「至少一位会长」不变式在改角色 / 禁用 / assign 三条路径上都有（`h_member.cj:190`、`:258`、`:303`）—— 对应 P09 `两步同时生效`、`社团必须始终保留至少一位会长`
- 口令与链接不写进业务日志（`audit` 只记"注册口令已更换"，不含口令值；`/join/{token}` 只输出部门名）—— 对应 P09 页尾 `不得落盘长存，也不得打印到设备日志`（服务端侧）
- `GET /auth/me` 服务端下发权限摘要（`h_auth.cj:459`）—— 对应 P10 `我的权限 · 由 GET /auth/me 下发`

**认证与注册（P11 / P12 / P13）**
- 登录：手机号 + 密码 → 签发 token；`AUTH_BAD_CREDENTIALS` 对"号码不存在"与"口令错误"返回同一码（`h_auth.cj:410`）；未注册号码也跑等价 PBKDF2 防时序枚举（N-16）
- 错误结构支持字段级定位：`error.fields`（`errors.cj:111`）—— 对应 P12 `字段级错误用红框加红字落在字段下方（error.fields）`
- 注册口令错误 → `REGISTER_CODE_INVALID` 400（`h_auth.cj:252`）—— 对应 P12 屏幕红字
- 注册口令失败节流：**按客户端 IP + 递增退避**（`h_auth.cj:63-129`），且与登录**各用一张表**、互不影响（`s.reg_fail` / `s.login_fail` / `s.login_fail_ip`）—— 对应 P12 `节流按客户端 IP，不影响他人，也不影响登录`
- 注册**即签发 token**，但账号是 `pending`、权限全 `false`（`h_auth.cj:272-283`）—— 对应 P12 `注册成功即返回 token，但账号是「待分配」、零权限`
- `pending` 账号：`view_scope = none`、所有布尔 false（`views.cj:58-85`）、可登录 / 可读 `/auth/me`（`requireMember`）、访问业务接口一律 `MEMBER_PENDING` 403 —— 对应 P13 `permissions 全部为 false、view_scope = none，所以不能进入主界面`
- **不做短信 / 邮箱自助找回**（全仓无相关端点）；忘记密码由管理员重置 —— 对应 P11 `v1 不做短信或邮箱自助找回`
- 登出即删 `token → member_id` 映射，旧 token 立即失效（`h_auth.cj:439`）—— 对应 P10 / P13 `退出登录`
- 本人改密走 `PUT /auth/password`，需验原密码，改完其他令牌全失效（`h_auth.cj:468`）
- 自签 HTTPS + TLS 1.2/1.3（TLS 22 项全绿）—— 对应 P11 `服务端为自签 HTTPS`

---

## 7. 建议的处理顺序

| 顺序 | 事项 | 谁做 | 是否阻塞客户端 |
| --- | --- | --- | --- |
| 1 | **D-3 / D-4 口径决策**（可见范围、要不要"需要帮助"） | 产品 / 设计 | 阻塞（决定后面改不改） |
| 2 | ~~D-6 密码口径~~ **已定稿并落地**（8–32 字节 + 数字/英文/符号） | 服务端 | — |
| 3 | **D-1 权限摘要补布尔** | 服务端 | 是（管理页做不了） |
| 4 | **D-2 名录搜索参数** | 服务端 + 设计确认语义 | 是（搜索框做不了） |
| 5 | **D-5 `overdue_days`**、**D-7 成员详情计数** | 服务端 | 否（客户端可绕，但会多打请求） |
| 6 | **D-8 待分配排序方向** | 一行改动 | 否 |
| 7 | **D-9 / D-10 / D-12 / D-14** 口头对齐即可 | 双方 | 否 |
| 8 | **E-1 ~ E-4** 修设计稿 | 设计 | 否 |

> ⚠️ 任何服务端改动都必须重跑四套测试（单测 / 冒烟 / TLS / 契约），
> 尤其 **D-3** 会动 `perms.cj` —— 那是全项目唯一的权限判定点，改完要重跑权限单测与冒烟。

---

## 8. 审计方法（可复现）

1. 设计 PDF **无文本层**（`pdffonts` 为空，文字全是矢量轮廓），所以按 300 dpi 渲染后**逐页视读**：
   ```powershell
   pdftoppm -png -r 300 "鸿蒙俱乐部-全场景UI设计规格.pdf" .cache\pdf300\page
   ```
   再按 1150 px 切成 `page-NN_c1..c4.png`（相邻片重叠 70 px），放在 `.cache\crops\`。
2. 13 页全部提取「有服务端后果」的要求（字段 / 分组排序 / 计数派生 / 状态枚举 / 权限角色 / 校验错误码 /
   查询参数 / 明确提到服务端的原话），输出到 `.cache\spec-notes-{A,B,C,D}.md`（A=01–03，B=04–06，C=07–09，D=10–13）。
3. **P01 / P02 / P03 / P06 / P08 / P12 的关键句由本人在原图上逐字复核**（全社团范围可见 · 需要帮助 · 搜索姓名或部门 ·
   待分配注册时间顺序 · 密码长度规则 · 服务端已排好序）；其余页引用来自第 2 步的逐字提取笔记，若需要可逐页再复核。
4. 服务端侧：读 `server/src/` 全部 24 个源文件的相关 handler / 视图 / 权限 / Store 函数，逐条对照；
   再实跑四套测试复现基线（§1）。

> `.cache/` 是未跟踪目录，可随时删除；删除后如需复查，按上面第 1 步重新渲染即可。

---

## 9. 待产品 / 设计确认清单

> ✅ 已定（2026-09-16）：**D-1 / D-2 / D-3 / D-4 / D-5 / D-7 / D-15 按设计规格改服务端**；
> D-8 / D-9 / D-10 ~ D-14 **保留原版本**；**D-6 已定稿为 8–32 字节 + 只允许数字/英文/符号**（有意偏离设计稿的 6 位下限）。以下仅剩真正待定的项。

- [x] **D-16（原队友第 8 条）✅ 2026-09-16 已闭环（选 1+2）**：
      ① 公开 JSON 端点 `GET /api/v1/join/{token}` → `{ dept: {id,name} | null, enabled }`；
      ② 注册带 `invite_token`，由**服务端**从 token 推出 `dept_hint`（优先级 token > 客户端自报 `dept_id`），
      响应回显 `dept_hint` 使"预填是否生效"可观察。
      > 令牌解析归服务端，客户端不再自报部门；链接失效**不阻断注册**（链接是便利，口令才是准入）。
- [x] **D-6 ✅ 2026-09-16 已定稿**：**8–32 字节**且**只允许数字 / 英文 / 符号**（可见 ASCII）。
      上限 32 采纳设计稿 P12，下限取 8（**有意偏离** P12 的 6），字符集为本次新增。
      ⚠️ 仍然**待设计侧确认**的只有一点：P12 那句「中文一字 3 字节」现在只影响长度怎么算——
      中文密码本身会被字符集拒绝，请设计把 P12 的示例文案同步过来。
- [ ] **D-8** 待分配列表：最新注册在前（PDF P08）还是先来先处理（`api-design §3.3`）？
- [ ] **D-9** 普通成员在名录里能否看到待分配成员的「来源」（`dept_hint`）？
- [ ] **D-12** 「一次性临时密码」是否要求"首次登录必须改密"？
- [ ] **D-14** 会长移交的「二次确认」由客户端做，还是服务端要有确认参数？
- [ ] **E-1 ~ E-4** 设计稿四处自相矛盾的数字 / 示例值修订（见 §5）
