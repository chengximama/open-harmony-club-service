# 社团管理工具 · 接口设计（REST API）

- 状态：**编写中**，分部分评审
- 依据：`v1-scope.md` v0.3
- 约定：本文档与技术栈无关。若服务端实现从仓颉/轻舟回退到其他语言，本文档不作废。

## 进度

| 部分 | 内容 | 状态 |
| --- | --- | --- |
| **Part 1** | **通用约定**（响应格式 / 错误码 / 认证 / 权限 / 时间 / 分页） | ✅ 已确认 |
| **Part 2** | **认证与注册**（注册口令、登录、我的信息） | ✅ 已确认 |
| **Part 3** | **组织与成员**（部门、名录、待分配审批、角色变更、会长移交、注册口令、招募链接） | ✅ 已确认 |
| **Part 4** | **任务**（我的任务、CRUD、状态流转、阻塞原因、日历同步） | ✅ 已确认 |
| **Part 5** | **课题**（多级树、递归进度、移动与环形校验、删除上提） | ✅ **待检查** |
| Part 6 | 接口—页面映射总表 + 权限对照复查 | ⏳ 待写 |

---

# Part 1 · 通用约定

## 1.1 基地址与版本

| 项 | 约定 |
| --- | --- |
| 前缀 | `/api/v1` |
| 传输 | HTTP/1.1，UTF-8 |
| 请求/响应体 | `application/json`（除文件类，v1 无文件） |
| 健康检查 | `GET /health`（**不带** `/api` 前缀，不需要认证） |

**为什么路径要带 `v1`**：客户端是装在成员手机上的 App，通过 AGC 分发。**你无法强制所有人升级**，一旦接口破坏性变更，旧版本 App 会直接崩。带版本号可以 v1/v2 并行跑，给旧客户端留退路。这不是洁癖，是移动端的硬约束。

## 1.2 统一响应格式

**成功**（HTTP 200 / 201）：

```json
{
  "ok": true,
  "data": { }
}
```

列表类 `data` 为分页对象，见 1.7。

**失败**：

```json
{
  "ok": false,
  "error": {
    "code": "TASK_NOT_FOUND",
    "message": "任务不存在",
    "fields": { "title": "标题不能为空" }
  }
}
```

**同时使用 HTTP 状态码与业务错误码**，两者分工明确：

- **HTTP 状态码**给网络层用：`401` → 统一踢回登录页，不用解析 body
- **`error.code`** 给业务层用：决定弹什么文案

| 状态码 | 语义 |
| --- | --- |
| 200 | 成功 |
| 201 | 创建成功 |
| 400 | 参数校验失败 / 注册口令错误 |
| 401 | 未认证（缺 token / token 失效 / 密码错） |
| 403 | 已认证但无权限 |
| 404 | 资源不存在 |
| 409 | 冲突（手机号已注册、部门重名） |
| 429 | 请求过于频繁（登录爆破防护） |
| 500 | 服务端错误 |

## 1.3 错误码表

命名规则：`<资源>_NOT_FOUND` / `FORBIDDEN_<原因>`，客户端可直接 `switch`。

| code | HTTP | 场景 |
| --- | --- | --- |
| `VALIDATION_FAILED` | 400 | 参数校验失败，`fields` 里给逐字段原因 |
| `REGISTER_CODE_INVALID` | 400 | 社团注册口令错误（见 Part 2 §2.3） |
| `AUTH_REQUIRED` | 401 | 缺 token |
| `AUTH_TOKEN_EXPIRED` | 401 | token 过期，客户端应静默跳登录 |
| `AUTH_BAD_CREDENTIALS` | 401 | 账号或密码错误 |
| `MEMBER_PENDING` | 403 | 账号已注册，但**尚未被会长分配部门与角色**（见 Part 2 §2.1） |
| `MEMBER_DISABLED` | 403 | 成员已退出社团 |
| `FORBIDDEN_ROLE` | 403 | 角色不足 |
| `FORBIDDEN_NOT_IN_DEPT` | 403 | 跨部门操作 |
| `MEMBER_NOT_FOUND` | 404 | |
| `DEPT_NOT_FOUND` | 404 | |
| `PLAN_NOT_FOUND` | 404 | |
| `TASK_NOT_FOUND` | 404 | |
| `ALREADY_EXISTS` | 409 | 手机号已注册、部门重名等 |
| `TOO_MANY_ATTEMPTS` | 429 | 登录失败次数过多 |
| `INTERNAL` | 500 | 兜底，**不向客户端暴露堆栈** |

> 回退方案保留：若日后换服务端实现，这张表照搬即可，客户端不用改。

## 1.4 认证

**方案：Bearer Token**（不用 Cookie）。

| 项 | 约定 |
| --- | --- |
| 请求头 | `Authorization: Bearer <token>` |
| 颁发 | 登录成功后返回 |
| 有效期 | **30 天**，每次请求滑动续期 |
| 存储 | 服务端以文件持久化 `token → member_id` 映射 |
| 注销 | `POST /api/v1/auth/logout` 使 token 立即失效 |

**为什么用 Bearer 而不是 Cookie**：客户端是鸿蒙 App 的 HTTP 客户端，不是浏览器。Cookie 的域/路径/`SameSite` 那套语义在这里只会添乱，而轻舟的 session 是基于签名 Cookie 的，需要额外适配。

**为什么是 30 天**：社团成员不是每天登录。有效期设成 1 天，等于每次打开 App 都要重新输密码，这是最容易被卸载的理由之一。

**为什么服务端要存 token 映射**：这样"退出社团"能做到**立即踢下线**。纯无状态 JWT 做不到这点——一个被移出社团的人能继续用旧 token 到过期为止。

## 1.5 安全底线（**需你确认**）

v1 走的是 `http://<公网IP>:8080`，**没有 HTTPS**。这意味着：

- 登录密码、token **以明文经过校园网/公网**
- 任何同网络的人抓包即可拿到 token，进而操作整个社团的数据

**建议：v1 必须上 HTTPS，哪怕是自签证书。** 代价是：

1. 服务端配自签证书（或申请免费证书，但需要域名）
2. **客户端要在 App 里配置信任该证书**，否则鸿蒙的 HTTP 客户端会直接拒绝连接

第 2 点是真实工作量，需要单独排。如果你认为社团内部试用阶段可以接受明文，请在评审时明确记录这个决定——**我不会默认帮你跳过它**。

## 1.6 权限判定

### 判定三层模型

每请求依次判定：

1. **认证**：有没有有效 token → 否则 `AUTH_REQUIRED`
2. **角色**：这个角色能不能做这个动作 → 否则 `FORBIDDEN_ROLE`
3. **部门范围**：目标资源是不是在本部门内 → 否则 `FORBIDDEN_NOT_IN_DEPT`

### 实现要求（重要）

**权限判断必须收敛到一个函数里**，例如：

```
can(member, action, target) -> bool
```

**绝不允许在每个 handler 里手写 if 判断。** 十几个接口各写一遍权限，一定会漏掉某个组合，而权限漏洞在社团工具里是最难被发现、后果最难受的一类 bug。

### 可见范围规则

| 角色 | 可读 | 可写 |
| --- | --- | --- |
| 会长 | 全社团一切 | 一切 + 设置角色 |
| 副会长 | 全社团一切 | 全社团任务与课题，**不能**设置角色 |
| 部长 / 副部长 | 本部门 | 本部门成员、任务、课题 |
| 成员 | **本部门全部**任务与课题 + 成员名录 | **只能改自己的任务状态与阻塞原因** |

**关于成员可读范围（需你确认）**：

建议让成员**能看本部门所有人的任务**（只读）。理由是这与第 1 节要解决的第二个问题直接相关——成员也需要能自己查到"这事谁负责"，不然会继续在群里问。如果只让成员看见自己的任务，这个工具对普通成员就只剩打卡功能。

## 1.7 分页

请求：`?page=1&size=50`

响应 `data`：

```json
{
  "items": [ ],
  "total": 128,
  "page": 1,
  "size": 50
}
```

| 项 | 约定 |
| --- | --- |
| `page` | 从 **1** 开始（不是 0） |
| `size` | 默认 50，最大 200，超出按 200 处理 |
| `total` | 满足筛选条件的总数 |

社团规模不大，分页主要为"任务会随学期累积"留余地。

## 1.8 时间与时区

| 项 | 约定 |
| --- | --- |
| 格式 | ISO 8601 带时区偏移：`2026-09-11T14:30:00+08:00` |
| 服务端存储 | **统一 UTC**，展示时转本地 |
| 可选时间 | 用 `null`，不用空字符串 |
| 小数秒 | **接受并截断**（服务端粒度到秒）。ArkTS / JS 的 `new Date().toISOString()` 产出 `2026-09-14T15:24:08.307Z`，客户端只要用标准库取"现在"就会带上毫秒 |
| 非法历法日期 | **拒绝**（400 `VALIDATION_FAILED`）。`2026-02-30`、平年的 `02-29` 一律不接，绝不静默顺延成 3 月 2 日 |
| 年份 | 4 位。排序/比较用 epoch 秒（Int64），不会有 2038 问题 |

**为什么强调时区**：`due_at` 要同步到系统日历，日历用的是绝对时间。时区处理错一次，提醒就会差 8 小时——而这类 bug 通常到截止前一刻才被发现，最伤信任。

## 1.9 幂等（创建类接口）

移动网络不稳定，用户点一下没反应就会再点一次，产生重复任务。

**约定**：所有 `POST` 创建类接口接受可选字段 `client_token`（客户端生成的 UUID）。

- 服务端记录 `(接口类型, 提交者, client_token) → 已创建资源 id`，**24 小时内**重复提交直接返回首次结果（HTTP 200，`data` 中附 `"duplicated": true`）
- 不传 `client_token` 则不做去重
- ⚠️ **幂等键必须含提交者**（2026-09-14 修订）。只按 `client_token` 索引时，
  甲用某个 token 创建了任务，乙拿同一个 token 重放就会被当成"甲的重复提交"，
  直接把甲的任务读回去——一个绕过权限判定的越权读（P0 缺陷，见 `code-review.md` H-2）。
  带上提交者后，乙的 token 匹配不到任何记录，会走正常创建流程，该 403 还是 403。
  命中回放前还会按**本次操作者**重新判一次读权限，作为防御纵深。

实现成本很低（一张临时映射表），但能挡掉"一个任务被创建三遍"这类最常见的脏数据。

## 1.10 通用请求头

| 头 | 必需 | 说明 |
| --- | --- | --- |
| `Authorization` | 除注册/登录/`/health` 外必需 | `Bearer <token>` |
| `Content-Type` | POST/PUT 必需 | `application/json` |
| `X-Client-Version` | 建议 | App 版本号，便于排查线上问题 |

服务端统一返回 `X-Request-Id`，日志按它串联——出问题时让成员报这一个 id 就能定位。

---

## 本部分需要你拍板的 4 处

| # | 事项 | 我的建议 |
| --- | --- | --- |
| 1 | **是否上 HTTPS** | **必须上**，自签证书也可；需接受客户端配信任的工作量 |
| 2 | **成员可见范围** | 成员**可读**本部门全部任务（只读），只可写自己的 |
| 3 | **token 有效期 30 天** | 采用 |
| 4 | **错误码命名规则** | `<资源>_NOT_FOUND` 形式，客户端可 switch |

---

# Part 2 · 认证与注册

## 2.0 前置更新：HTTPS 已有可用实现

轻舟已在最新版加入 `serveTls` / `listenTls`。本机实测结果：

| 项 | 结果 |
| --- | --- |
| TLS 1.2 / 1.3 握手 | ✅ 通过 |
| TLS 1.0 / 1.1 | ✅ 被服务端拒绝（`tlsv1 alert protocol version`） |
| 真实 HTTPS 请求 | ✅ `HTTP/1.1 200 OK` |

**这意味着 §1.5 的 HTTPS 要求不再需要额外引入 Nginx 即可满足。**

但当时发布版本**编译不过**（缺失 import、且调用了 stdx 1.1.3.1 中未实现的静态方法），缺陷与验证过程见上层 `cangjie-upstream\qingzhou-tls-verification.md`。

**决定：采用框架原生 TLS，不再引入 Nginx。** 已于 2026-09-13 向轻舟团队反馈 DEF-1；**2026-09-14 上游 `141a735` 已修复同一处，本地补丁已撤**，随后实测 TLS 关卡 **22 / 0 全绿**（见 `HANDOFF.md` §11 与 `API-NOTES.md` 的环境说明）。

## 2.1 账号与成员身份分离（核心模型）

### 登录账号 = 手机号

**不用学号**：社团包含**校外人员**（企业指导老师、已毕业学长），他们没有在校学号。手机号人人都有、天然唯一、好记。

### 注册与授权解耦

这是本版相对初稿**最重要的结构改动**：

```
注册 ──▶ [pending 待分配] ──会长分配部门+角色──▶ [active 正式成员]
          · 无部门、无角色、无任何权限
          · 登录后只能看到「等待管理员分配」页面
```

账号与权限的**生命周期完全独立**：

| 场景 | 处理 |
| --- | --- |
| 新人加入 | 注册 → 会长分配 |
| **换届** | **只改角色，账号不动** ← 本次改动要解决的核心问题 |
| 成员退出 | `status = disabled`，账号保留，日后可恢复 |
| 毕业学长回来 | 账号还在，重新赋予角色即可 |

### 手机号不做短信验证

短信有费用、要资质，对社团规模不划算。**防冒用由会长的授权审核环节兜底**——每个账号都必须经过人工分配才能获得任何权限，冒用他人手机号会在这一步暴露。

正因为引入了解耦后的待分配状态，不做短信验证才是可接受的；如果注册即获得权限，就必须上验证。

### 数据模型调整（需同步修订 `v1-scope.md` §5）

| 字段 | 说明 |
| --- | --- |
| `phone` | **登录账号**，全局唯一；同时就是联系方式（取代原 `contact`） |
| `password_hash` | 密码哈希，不落明文 |
| `status` | 由「正常 / 已退出」改为 **`pending` / `active` / `disabled`** |

> 初稿曾建议用 `student_id` 做登录账号，**该建议已撤销**。

**密码**：服务端存哈希（用 stdx 的 crypto），任何情况下不落明文、不进日志。

## 2.2 接口总览

| 方法 | 路径 | 需认证 | 用途 |
| --- | --- | --- | --- |
| POST | `/api/v1/auth/register` | 否 | 凭注册口令注册，**注册即登录**，得到 `pending` 账号 |
| POST | `/api/v1/auth/login` | 否 | 手机号 + 密码登录 |
| POST | `/api/v1/auth/logout` | 是 | 使当前 token 立即失效 |
| GET | `/api/v1/auth/me` | 是 | 当前成员信息 + 权限摘要 |

## 2.3 POST /auth/register

**请求**

```json
{
  "register_code": "社团注册口令",
  "phone": "13800000000",
  "name": "张三",
  "password": "至少 8 位",
  "dept_id": 2,
  "client_token": "uuid"
}
```

`dept_id` 与 `client_token` 可选。

**响应 201**

```json
{
  "ok": true,
  "data": {
    "token": "…",
    "expires_at": "2026-10-13T10:00:00+08:00",
    "member": { "…MemberBrief…", "status": "pending" },
    "permissions": { "view_scope": "none", "…其余全部 false…" }
  }
}
```

**错误**

| code | HTTP | 场景 |
| --- | --- | --- |
| `VALIDATION_FAILED` | 400 | 字段级原因放 `fields` |
| `REGISTER_CODE_INVALID` | 400 | 注册口令错误 |
| `ALREADY_EXISTS` | 409 | 手机号已注册 |

**说明**

1. **注册即登录**：直接签发 token。但账号是 `pending`，权限全为 `false`，**客户端应跳转「等待管理员分配」页，而不是主界面**
2. **注册口令只决定"能否注册"，不决定任何角色。** 它是社团共享口令，会长可在管理页随时更换。相比逐个发放一次性邀请码，**换届时不需要重发**，新成员问一句就能注册
3. **`dept_id` 来自部门招募链接**（如「运营部招新」）。它**只做部门预填**，角色一律默认 `member`，且**仍需会长确认**才转为 `active`。这样既减轻会长负担，又不让任何人凭一个链接自动获得成员身份
4. ⚠️ 请求体**不接受** `role`。任何角色都必须由会长在管理界面授予——这是防越权的关键，否则任何人都能把自己注册成会长
5. 密码只校验长度（≥ 8 位），不强制大小写数字混合——社团内部工具，复杂度要求只会逼出 `Aa123456` 这种更差的密码
   > **口径说明（2026-09-13 确认）**：长度按**字节**计（服务端实现中 `String.size` 即字节数），
   > 即中文一个字符算 3 字节。这是实现层的既定口径，v1 不做字符数统计；对社团内部工具的密码强度影响可忽略。
6. **注册口令长度下限 6 位，且注册接口按客户端 IP 做失败节流**（2026-09-14 二次修订：无节流 → 全局节流 → **按 IP + 递增退避**）。
   口令是**全社团共用的单一秘密**，只需猜中一次就能拿到账号，4 位空间只有约 45 万种，
   在线爆破是小时级的事。规则：同一 IP 在 15 分钟内口令错 10 次 → 锁定该 IP
   （返回 `TOO_MANY_ATTEMPTS`）；锁定时长**递增退避**：首次 5 分钟，同一 IP 每再触发一次翻倍，上限 1 小时。
   > **按 IP，而不是全局**（2026-09-14 更正）。本节此前写"轻舟没有暴露可信的客户端 IP，
   > 伪造 `X-Forwarded-For` 即可绕过"——**这句是错的**，它把两个概念混在了一起：
   > 轻舟通过 `ctx.http.request.remoteAddr` 暴露 **TCP 对端地址**（即 `h_ops.cj` 的 `peerOf` / `clientIpOf`），
   > 那是传输层事实，客户端伪造不了；可伪造的是 `X-Forwarded-For` **请求头**，
   > 而本项目直连部署、根本不读它。
   > 实测该字段的文本形如 `IP:端口`（例 `127.0.0.1:14764`），所以按 IP 分桶前**必须先剥掉端口**，
   > 否则同一台机器每个连接都是新 key，限流形同虚设。
   > 旧全局锁的真实代价：攻击者每 15 分钟丢 10 个错口令，就能把**所有人**挡在注册门外。
   > **已知局限**（别高估）：NAT / 校园网出口下整个社团可能共用一个公网 IP，那时按 IP 会近似退化成全局锁；
   > 取不到对端地址时 key 为空串，同样退化为全局。两种情况都比"无条件全局锁"更窄。
   > 另外，按 IP **不阻止分布式尝试**（IPv6 在一个 /64 内轮换源地址尤其便宜），
   > 所以它挡不住"换着源地址爆破"，安全性最终依赖口令长度（≥ 6 位，26⁶ ≈ 3.09 亿种）。
   > 别把"按 IP"读成"不可爆破"。
   > 锁定**只影响注册**，不影响登录与其它接口；也**没有新增"解除锁定"接口**——改成按 IP 之后，
   > 被锁的只有攻击者自己那个地址，正常用户不受影响，不需要会长介入。
   > 节流状态**只存在内存**（不落盘）：失败是高频路径，每失败一次就写一次整库本身就是可被利用的放大杠杆。
   > 重启后计数清零可以接受——攻击者没有让服务重启的手段。

## 2.4 POST /auth/login

**请求**

```json
{ "phone": "13800000000", "password": "…" }
```

**响应 200**：`data` 与 register 相同（`token` + `expires_at` + `member`）

**错误**

| code | HTTP | 场景 |
| --- | --- | --- |
| `AUTH_BAD_CREDENTIALS` | 401 | 手机号不存在**或**密码错误 |
| `MEMBER_DISABLED` | 403 | 成员已退出社团 |
| `TOO_MANY_ATTEMPTS` | 429 | 失败次数过多 |

**说明**

- ⚠️ **"手机号不存在"与"密码错误"必须返回同一个错误码**，不得泄露某手机号是否注册过
- ⚠️ **登录限流必须做**。这个服务会暴露在公网，登录接口一定会被撞库扫描。规则：同一手机号 15 分钟内失败 5 次 → 锁定 15 分钟。实现很简单，但 v1 里省不掉
- **`pending` 账号可以正常登录**，只是 `permissions` 全为 `false`、`view_scope` 为 `none`。客户端据此显示「等待管理员分配」页，**不要**当成错误踢回登录页

## 2.5 POST /auth/logout

请求体为空。响应 `200 { "ok": true }`。

**说明**：服务端删除 `token → member_id` 映射，旧 token **立即**失效。这正是 Part 1 §1.4 选择"服务端存 token 映射"而非纯无状态 JWT 的原因——成员退出社团或被移出后，能立刻踢下线。

## 2.6 GET /auth/me

**响应 200**

```json
{
  "ok": true,
  "data": {
    "member": { "…MemberBrief…" },
    "permissions": {
      "view_scope": "dept",
      "manage_members": false,
      "set_role": false,
      "create_plan": false,
      "create_task": false,
      "update_any_task": false
    }
  }
}
```

**说明**：这是客户端启动时调的第一个接口，一石二鸟：

1. **校验 token 是否仍有效**（返回 401 就跳登录页，不需要额外接口）
2. **拿到权限摘要**，客户端据此决定 UI 显示什么

> 权限判定**只保留服务端一份**。客户端读 `permissions` 是为了画界面，绝不能用它替代服务端校验——真正的拦截仍在 `can(member, action, target)`（见 Part 1 §1.6）。

## 2.7 公共数据结构

### MemberBrief

后续所有接口涉及"人"的地方统一用这个结构。

| 字段 | 类型 | 说明 |
| --- | --- | --- |
| `id` | int | |
| `name` | string | 姓名 |
| `role` | string \| null | `president` / `vice_president` / `lead` / `vice_lead` / `member`；**`pending` 时为 `null`** |
| `dept` | object \| null | `{ "id": 2, "name": "运营部" }`；**`pending` 时为 `null`** |
| `status` | string | `pending`（待分配）/ `active`（正式）/ `disabled`（已退出） |
| `joined_at` | string | ISO 8601 |

> ⚠️ **MemberBrief 不含手机号。** 成员名录只需要姓名、部门、角色。手机号属于个人信息，只在 `/auth/me`、本人资料，以及会长/副会长可见的管理视图中出现。这是成本为零的隐私最小化。

### Permissions

| 字段 | 类型 | 说明 |
| --- | --- | --- |
| `view_scope` | string | `all` / `dept` / `self` / **`none`**（`pending` 账号） |
| `manage_members` | bool | 可增删成员、调部门 |
| `set_role` | bool | 仅会长为 `true` |
| `create_plan` | bool | 可创建课题 |
| `create_task` | bool | 可创建 / 分配任务 |
| `update_any_task` | bool | 可改他人任务状态（成员为 `false`，只能改自己的） |

## 2.8 决策与待办

### 已确认（2026-09-13）

| # | 事项 | 结论 |
| --- | --- | --- |
| 1 | 登录账号 | **手机号**（社团含校外人员，不用学号） |
| 2 | 注册门槛 | **社团共享注册口令**，会长可随时更换 |
| 3 | 进部门方式 | **部门招募链接 + 批量分配** |
| 4 | 账号与授权 | **解耦**：注册后 `pending`，角色全部由会长授予 |
| 5 | 是否采用框架原生 TLS | **采用**，不再引入 Nginx（所依赖的轻舟 DEF-1 **已由上游 `141a735` 修复**，本地补丁已撤） |
| 6 | MemberBrief 不暴露手机号 | 采用 |
| 7 | 登录限流阈值 | 同一手机号 15 分钟 5 次失败 |
| 8 | **首任会长如何预置** | **初始化命令**，例如 `club-server.exe init-admin <手机号> <初始密码>`。**不采用**"首个注册者自动成为会长" |
| 9 | 术语 | 定为「**课题**」（沿用甲方表格用词），字段名统一 `plan` |
| 10 | 课题层级 | **可层层往下分割（多级）**：课题可挂子课题，任务为叶子且不再分层。详见 `v1-scope.md` v0.5 与 Part 5 |

### 仍待确认

| # | 事项 | 我的建议 |
| --- | --- | --- |
| 1 | 忘记密码 | v1 **不做**自助找回（要短信/邮箱，成本高）。由会长在管理页重置为临时密码 |

### 移交给 Part 3 的接口

以下为本次改动**新增**，将在 **Part 3 · 组织与成员** 中定义：

- 待分配列表、单个分配、**批量分配**
- 角色变更、**会长移交**（含"系统必须始终保留至少一个 `president`"的约束）
- 注册口令的查看与更换
- 部门招募链接的生成与管理
- 重置成员密码

---

# Part 3 · 组织与成员

## 3.0 本部分范围

Part 3 比最初设想的大得多——因为「注册与授权解耦」之后，**审批与授权全部落到这里**：

| 分组 | 内容 |
| --- | --- |
| 3.1 部门（组织） | 增删改查 |
| 3.2 成员名录 | 名录、详情、编辑、移出 |
| 3.3 待分配与授权 | 待分配列表、单个分配、**批量分配** |
| 3.4 会长移交 | 转让 `president` |
| 3.5 密码 | 本人改密、会长重置 |
| 3.6 注册配置 | 注册口令的查看与更换 |
| 3.7 部门招募链接 | 生成与管理 |

## 3.1 部门（组织）

### GET /depts

**权限**：全体 `active` 成员

**响应 200**

```json
{
  "ok": true,
  "data": {
    "items": [
      { "id": 2, "name": "运营部", "sort": 2,
        "lead": { "…MemberBrief…" },
        "member_count": 8 }
    ]
  }
}
```

不分页（只有 4 个组织）。

### POST /depts

**权限**：**仅会长**（2026-09-13 决策：部门增删改全部会长独占，见 3.8 与本页注）

```json
{ "name": "技术部", "sort": 4 }
```

**错误**：`VALIDATION_FAILED`（400）、`ALREADY_EXISTS`（409，重名）

### PATCH /depts/{id}

**权限**：**仅会长**（2026-09-13 决策）

请求体字段全部可选：`name` / `sort`。

> ⚠️ **不接受 `lead_id`。** 见 3.8 的待拍板项 1——我们建议**删除 `Department.lead_id`**，
> 部长身份只由 `Member.role = lead` 表达。同一个事实存两份必然会不一致。

### DELETE /depts/{id}

**权限**：会长

| code | HTTP | 场景 |
| --- | --- | --- |
| `DEPT_NOT_EMPTY` | 409 | 部门下还有成员，**必须先清空** |

**说明**：不允许删除非空部门。社团的组织架构变更应该是有意识的，不能一键把 8 个人变成无部门状态。

> **实现补充（2026-09-13）**：`DEPT_NOT_EMPTY` 的判定范围比原文档更宽，否则会留下悬空引用——
> 除**未退出成员**外，**顶层课题**也计入（`Plan.dept_id` 只存在顶层，见 5.1 规则 A）。
> 删除时对**已退出成员**与**历史任务**的部门标签**置 0**（记录本身保留）：
> 与规则 17「任务承载工作量记录」同源，不删除任何记录，但也不留指向已删部门的 id。

## 3.2 成员名录

### GET /members

**权限**：全体 `active` 成员

**Query**：`dept_id` / `role` / `status` / `page` / `size`（默认只返回 `active`）

**响应 200**：分页的 `MemberBrief` 列表

> **建议：成员名录对全体成员开放（全社团可见）。**
> 理由：这是第 1 节要解决的**第一个问题**——「社团里有哪些人」。如果成员只能看自己部门，这个问题就没被解决。
> 注意区分：**名录全社团可见，但任务与课题仍按部门限制**。这两件事的可见性不必一致。

### GET /members/{id}

**权限**：全体 `active` 成员

**响应 200**：`MemberBrief` + 计数摘要

```json
{
  "ok": true,
  "data": {
    "member": { "…MemberBrief…" },
    "stats": { "owned_tasks": 3, "open_tasks": 2, "owned_plans": 1 }
  }
}
```

只给**计数**，不展开任务明细——明细属于 Part 4 的列表接口。

### PATCH /members/{id}

**权限**：见 3.8 的权限表

请求体字段全部可选：`name` / `dept_id` / `role`。

> **字段级权限（2026-09-13 明确）**：`name` 走 **编辑成员姓名**（部长/副部长限本部门），
> `dept_id` / `role` 走 **调部门/改角色**（仅会长、副会长）。两者权限不同，
> 不能因为它们在同一个请求体里就合并判定。
>
> **待分配成员**只接受 `name`；对它传 `dept_id` / `role` 返回 `VALIDATION_FAILED`，
> 提示改用 `POST /members/{id}/assign`——避免两条路径各写一半授权逻辑。

**错误**

| code | HTTP | 场景 |
| --- | --- | --- |
| `FORBIDDEN_LAST_PRESIDENT` | 409 | 试图把**最后一个** `president` 降级 —— 见 `v1-scope.md` 规则 10 |
| `VALIDATION_FAILED` | 400 | |

### POST /members/{id}/disable

**权限**：会长 / 副会长

**说明**：把 `status` 置为 `disabled`，**账号与历史记录全部保留**（不删除）。该成员名下未完成的任务需要先转移——见下方错误。

| code | HTTP | 场景 |
| --- | --- | --- |
| `MEMBER_HAS_OPEN_TASKS` | 409 | 名下有未完成任务，响应里给出任务列表，要求先转交 |
| `FORBIDDEN_LAST_PRESIDENT` | 409 | 不能禁用最后一个会长 |

> **`MEMBER_HAS_OPEN_TASKS` 的响应形状（2026-09-13 明确）**：任务清单放在
> `error.fields.tasks`，每项为 `{ "id": 12, "title": "…", "status": "doing" }`。
> 这样不必为错误响应新增字段、也不改动统一包封——客户端按 `code` 分支后直接读 `fields.tasks` 即可。
>
> **幂等**：对已经 `disabled` 的成员重复调用返回 200（客户端重试不该产生错误）。

> 不允许"人走了任务跟着烂尾"。这也是本工具要解决的问题——**谁负责，得有人接手**。

## 3.3 待分配与授权

### GET /members/pending

**权限**：会长 / 副会长（可选增强见 3.8 待拍板项 4）

**响应 200**：`pending` 成员列表，按注册时间正序（先来先处理）

```json
{
  "ok": true,
  "data": {
    "items": [
      { "…MemberBrief…": "", "status": "pending",
        "dept_hint": { "id": 2, "name": "运营部" },
        "registered_at": "2026-09-13T10:00:00+08:00" }
    ]
  }
}
```

`dept_hint` 来自部门招募链接，**只是提示**，不是已生效的部门。

### POST /members/{id}/assign

**权限**：会长 / 副会长

```json
{ "dept_id": 2, "role": "member" }
```

**响应 200**：更新后的 `MemberBrief`（`status` 变为 `active`）

**说明**：**幂等**。对已是 `active` 的成员重复调用同样的分配，返回 200 而不报错——客户端重试不该产生错误。

> 对 `disabled` 成员调用即**恢复**：账号一直在，重新赋角色即可（见 §2.1 的生命周期表）。
> 分配成功后 `dept_hint` 清空——预填信息已完成使命。
>
> ⚠️ **恢复必须可追溯（2026-09-14 明确，见 `code-review.md` M-4）**。`disable` 保留全部数据、
> 文档也写明"日后可恢复"，所以本接口不拒绝 `disabled` 目标——但恢复与普通分配**混成了同一个语义**：
> 没有专门接口、没有额外确认，审计日志里也只记成普通的分配。
> 现状按"保持行为、补全可追溯性"处理：
> 1. 目标原本是 `disabled` 时，审计动作名记为 **`restore-member`**（普通分配仍是 `assign-member`），
>    note 里写明"恢复已退出成员"；
> 2. `assign-batch` 的审计 note 增加"其中恢复已退出成员=N"；
> 3. 本行为在此**明确成文**，不再是实现先跑、文档后补的空白区。
>
> 如果后续认为"恢复"应当是一个需要更高确认度的动作，第 2 个方案是给它一个独立入口
> （如请求体带 `restore: true`，或单独的管理接口）——那属于 v1 之后的范围扩展，
> 会改变接口清单数量，需先确认。

> ⚠️ **`role` 不得为 `president`**（2026-09-13 补充）。
> 会长**只能由 `POST /members/{id}/transfer-presidency` 产生**——那是唯一的原子移交路径。
> 若允许本接口直接授予 `president`，就会出现两个会长，规则 10「系统必须始终至少有一个 `president`」
> 与规则 11「会长移交是一个明确操作」同时失去意义。传入 `president` 返回 `VALIDATION_FAILED`。

### POST /members/assign-batch

**权限**：会长 / 副会长

```json
{ "member_ids": [11, 12, 13], "dept_id": 2, "role": "member" }
```

**响应 200**——**逐条报告结果，允许部分成功**：

```json
{
  "ok": true,
  "data": {
    "succeeded": [11, 12],
    "failed": [ { "id": 13, "code": "MEMBER_NOT_FOUND" } ]
  }
}
```

> ⚠️ **批量接口绝不能"一条失败就整体回滚"。** 招新期一次处理 20 个人，因为其中 1 个数据有问题就全部不生效，
> 会长会疯掉。逐条报告、让成功的生效，才是可用的批量语义。

**这是招新期最重要的一个接口**——它直接决定第 2 节验收标准 1 能否达成。

## 3.4 会长移交

### POST /members/{id}/transfer-presidency

**权限**：**仅现任会长**

```json
{ "self_role": "vice_president" }
```

**响应 200**

**说明**：这是**一个原子操作**，内部同时完成：

1. 目标成员 `role → president`
2. 自己 `role → self_role`（默认 `vice_president`）

> ⚠️ **这是产生会长的唯一入口**（2026-09-13 补充）。
> `POST /members/{id}/assign` 与 `PATCH /members/{id}` 一律**拒绝** `role = president`，
> 因此系统里换会长只有这一条路可走：**原子、两步同时生效、不会出现零会长窗口**。

> ⚠️ **为什么必须是一个接口，而不是"改两次角色"？**
> 分两步做，中间会出现**零会长**的状态。虽然只是短暂窗口，但如果第二步失败（网络、进程重启），
> 社团就永久失去管理能力，只能改服务器数据文件——见 `v1-scope.md` 规则 10 与 11。

**错误**

| code | HTTP | 场景 |
| --- | --- | --- |
| `MEMBER_PENDING` | 403 | 目标成员尚未被分配部门与角色，不能接任 |
| `MEMBER_DISABLED` | 403 | 目标成员已被移出社团，不能接任 |
| `VALIDATION_FAILED` | 400 | 目标就是自己 |

> **2026-09-14 修订（`code-review.md` L-8）**：`pending` 与 `disabled` 原先共用 `MEMBER_PENDING`，
> 客户端会提示"尚未被分配"，而真实原因是"已被移出社团"。两种状态给两个码，
> 与 `POST /auth/register` / 任务创建路径保持一致。

## 3.5 密码

### PUT /auth/password（本人改密）

**权限**：本人

```json
{ "old_password": "…", "new_password": "…" }
```

改密成功后**当前 token 之外的其他 token 全部失效**（见 Part 1 §1.4）。若怀疑账号被盗，这是有效的自救手段。

| code | HTTP | 场景 |
| --- | --- | --- |
| `AUTH_BAD_CREDENTIALS` | 401 | 旧密码错误 |

### POST /members/{id}/reset-password

**权限**：会长 / 副会长 / 部长 / 副部长，但**只能作用于档位低于自己的人**——
**不可重置与自己同权或更高权的人**（对自己而言就是同权，因此任何人都不能用本接口改自己的密码）。
本人改密一律走 §3.5 的 `PUT /auth/password`（需原密码）。

**响应 200**

```json
{ "ok": true, "data": { "temporary_password": "k7m2p9qd" } }
```

**说明**：

- 服务端**随机生成**临时密码，**只在这一处返回一次**，之后不再可查
- 这是 v1 里"忘记密码"的唯一出路（不做短信 / 邮箱自助找回）
- 被重置者用临时密码登录后，应自行用 `PUT /auth/password` 改掉
- 该操作**记入日志**（谁在什么时候重置了谁）——这是敏感操作，需要可追溯
- ⚠️ **本接口不含"本人"**（2026-09-14 口径统一）。对本人而言与操作者同权，正是下面那条
  通用规则的第一个特例。本人改密走 §3.5 的 `PUT /auth/password`
  （需要验原密码，更安全）；本接口签发一次性临时密码，属于**管理员救济动作**。
  此前 §3.8 汇总表与 `v1-scope.md` 曾把成员列写成「仅本人」，与本节及 §6.1 自相矛盾，现已统一。
- ⚠️ **不能作用于会长本人**，且部长 / 副部长**只能作用于档位低于自己的成员**
  （部长动不了另一位部长，也动不了副会长与会长，即使目标与自己同部门）。
  这条约束原先只写在副会长那一档，导致同部门的部长可以重置会长密码并接管账号，
  已作为 P0 缺陷修复（见 `code-review.md` H-1）。
- ⚠️ **不可重置与自己同权或更高权的人**（2026-09-14 定，见 `code-review.md` N-7）。
  这条通用规则一次覆盖三种情况：
  - **对自己同权** → 任何人都不能用本接口重置自己（会长 / 副会长 / 部长都一样）。
    此前"不含本人"只是文档与注释口径，代码里并没有拦：会长与副会长的角色分支都是
    **直接放行**，所以他们重置**自己**会成功；而该操作收尾会把目标的全部令牌作废，
    表现为"响应 **200**、自己当场掉线"（客户端拿到成功，下一个请求却 401）。
  - **副会长对副会长同权** → 同权之间不能互相踢下线（原先没有这条约束）。
  - **会长档位最高**，除自己外无人与之同权 → 会长仍能重置任何更低档的人。
  约束落在 `perms.cj` 的 `checkAccess`（**动作语义层**），不靠各 handler 自觉 ——
  权限判定仍然只有一处。
  > 该规则随后**推广到"调部门 / 改角色"、"移出社团"、"编辑姓名"**（同一天），
  > 因为只保护重置密码时，副会长仍可把另一位副会长降级或禁用。详见 §3.8 的
  > 2026-09-14 修订第 3 条。
- ⚠️ **本人改自己密码的通道不变**：`PUT /auth/password`（会长也一样，需原密码）。
  唯一的边界是"彻底忘记原密码"——该通道要验原密码，此时 v1 没有自助找回
  （见 `README.md` 未决事项 #4），不在本节范围内。

## 3.6 注册配置

### GET /register-config

**权限**：会长 / 副会长

```json
{
  "ok": true,
  "data": {
    "code": "K3M9P2",
    "updated_at": "2026-09-13T10:00:00+08:00",
    "updated_by": { "…MemberBrief…" }
  }
}
```

### PUT /register-config/code

**权限**：会长

```json
{ "code": "新口令" }
```

**说明**：更换后**旧口令立即失效**。正在注册的人会失败——建议在群里公告新口令时同步说明。

> **长度限制（2026-09-14 修订）**：**6 至 32 位**（原下限为 4 位）。口令是全社团共用的单一秘密，
> 只需猜中一次就能拿到账号；4 位空间只有约 45 万种，配合 `POST /auth/register` 的失败节流
> （15 分钟错 10 次即锁定）才构成可接受的在线爆破成本。上限 32 位是为了让它能在群里念出来。

### POST /register-config/rotate

**权限**：会长

服务端随机生成一个易读的新口令并返回。用于"口令可能外泄，但想不出新的"时一键轮换。

## 3.7 部门招募链接

### GET /dept-invite-links

**权限**：会长 / 副会长

### POST /dept-invite-links

**权限**：会长 / 副会长

```json
{ "dept_id": 2 }
```

**响应 201**

```json
{
  "ok": true,
  "data": {
    "token": "a1b2c3d4",
    "url": "https://<host>/join/a1b2c3d4",
    "dept": { "id": 2, "name": "运营部" },
    "enabled": true
  }
}
```

### DELETE /dept-invite-links/{token}

**权限**：会长 / 副会长 —— 停用链接（招募结束后关闭）

> **语义是"停用"而不是删除记录（2026-09-13 明确）**：置 `enabled = false`，记录保留，
> 便于在列表里看到"已停用的秋招链接"。与 `GET /dept-invite-links` 的 `enabled` 字段一致。
>
> **幂等**：对不存在或已停用的 token 重复调用一律返回 **200**——与 `assign` 同理，
> 客户端重试不该产生错误（也因此不需要引入新的"链接不存在"错误码）。
>
> **响应形状统一（2026-09-14 修订）**：链接不存在时也返回**同一个视图**
> （`token` 回显、`enabled: false`、`dept` 为空对象、`created_at: null`），
> 原先返回空对象 `{}`，客户端要多写一个"有没有 `data.link`"的特例。

**说明**：

- 招募链接**只做部门预填**，**不赋予任何角色**。通过它注册的人仍是 `pending`，必须经审批才转为 `active`
- **链接不免除注册口令**。设计上保持**单一门槛**：链接是便利，口令才是准入。否则就有了两套平行凭证，泄露时要想两处
- 每个部门可以有多个链接（如"2026 秋招"和"补录"），便于分别停用与统计来源

### GET /join/{token}（链接落地页，**不在 /api/v1 下**）

**权限**：公开（无需登录）

返回一个最小可用的 HTML 说明页，告诉打开链接的人这个链接属于哪个部门、下一步该做什么。

**说明**：

- 客户端是 HarmonyOS 应用，浏览器打开链接**无法**直接唤起 App（v1 不做 deep link 配置），
  所以落地页必须自己存在——否则招募链接点开就是 404，而它是要被发到群里的
- 页面**只暴露部门名**：那本来就是链接持有者能看到的信息，不含口令、成员或任务数据
- 链接已停用或不存在时同样返回 200，页面提示"链接已失效"
- 生成的 `url` 用请求的 `Host` 头拼装（避免把域名写死进配置）。
  ⚠️ 已知取舍：`Host` 头是调用方可控的，响应里的 `url` 会被污染——只影响调用方自己的响应，
  不落盘、不影响他人，因此定为低危；正式上线时应改为服务端配置项

## 3.8 权限汇总表（**提议，需确认**）

Part 3 的接口权限，按如下模型。**这张表细化了 `v1-scope.md` §3.4.3，有两处与原表不同，见待拍板项 1。**

| 能力 | 会长 | 副会长 | 部长 | 副部长 | 成员 |
| --- | --- | --- | --- | --- | --- |
| 查看成员名录 | 全社团 | 全社团 | 全社团 | 全社团 | 全社团 |
| 编辑成员姓名 | ✅ | ✅ | 本部门、档位更低 | 本部门、档位更低 | — |
| 调部门 / 改角色 | ✅ | ✅ ⚠️ | — | — | — |
| 移出社团 (`disable`) | ✅ | ✅ ⚠️ | — | — | — |
| 审批待分配 / 批量分配 | ✅ | ✅ | — | — | — |
| 重置成员密码 | ✅ | ✅ | 本部门、档位更低 | 本部门、档位更低 | — |
| 部门增删改 | ✅ | — | — | — | — |
| 查看 / 更换注册口令 | ✅ | 仅查看 | — | — | — |
| 招募链接管理 | ✅ | ✅ | — | — | — |
| **移交会长** | ✅ | — | — | — | — |

**⚠️ 副会长一列的两点说明**（对应待拍板项 1）：提议副会长**除"移交会长"、"改动会长本人"、"更换注册口令"、"部门增删改"外，与会长同权**。原表把副会长的"管理成员"标为 `—`，但甲方对副会长的描述是"协助会长管理社团"，标 `—` 会让这个身份名不副实。

> **2026-09-13 修订（评审中发现文档自相矛盾）**：本节原写「部门增删改 ✅✅」，
> 而 §3.1 的分接口权限与 §6.1 清单都写「DELETE /depts/{id}：会长」。
> 经确认**统一为会长独占**：组织架构是社团层面的事，增删改都会影响所有人的归属，
> 副会长可以管人（调部门、改角色），但不直接改动组织本身。§3.1、§6.1、本表三处已一致。

> **2026-09-14 修订（两处口径统一 + 一条档位规则）**：
> 1. 「重置成员密码」的成员列由「仅本人」改为「—」。本人改密走 `PUT /auth/password`
>    （§3.5），本接口是管理员救济动作、签发一次性临时密码。原来 §3.5 正文与 §6.1 都写"不含本人"，
>    只有本表与 `v1-scope.md` §3.4.3 写「仅本人」——四处口径现已统一为**不含本人**。
> 2. 「编辑成员姓名」与「重置成员密码」对部长 / 副部长追加**档位限制**：
>    只能作用于**档位低于自己**的成员（会长 &gt; 副会长 &gt; 部长 = 副部长 &gt; 成员）。
>    这是 P0 缺陷 H-1 的修复口径：原先"不可改动会长本人"只写在副会长那一档，
>    同部门的部长可以重置会长密码并接管账号。
>    例外是**改自己的姓名**——改名不改变角色、不构成提权，且本部门当然包含自己。
> 3. **「不可作用于与自己同权或更高权的人」推广到全部成员处置类动作**（2026-09-14）：
>    **重置密码 / 调部门改角色 / 移出社团 / 编辑姓名**一律适用。
>    此前只保护了"重置密码"，而**副会长**档在这四类动作里只排除会长本人，
>    于是副会长可以把**另一位副会长**降级为 `member`、直接禁用或改名 ——
>    回退实测这三条全部返回 `200`。现在它们统一由 `perms.cj` 的 `checkAccess`
>    （**动作语义层**）拦下，同权之间不能互相处置。
>    因此**本表中副会长那一列的「✅」都应读作"仅限档位低于自己的人"**；
>    会长档位最高、除自己外无人与之同权，仍能作用于任何更低档的人。
>    唯一的例外是**对自己**：只禁"重置密码"这一项（它收尾会作废目标的全部令牌，
>    对本人执行就是"响应 200、自己当场掉线"，见 N-7）；改自己的姓名 / 角色 / 启停
>    属既有口径，且是自伤、没有提权收益，不受这条约束。

## 3.9 本部分待拍板

| # | 事项 | 我的建议 |
| --- | --- | --- |
| 1 | **副会长的权限范围** | ~~除"移交会长"和"改动会长本人"外，与会长同权~~ → **已确认（2026-09-13）**：除「移交会长」「改动会长本人」「更换注册口令」「部门增删改」外与会长同权。已同步 §3.1、§3.8、§6.1 与 `v1-scope.md` §3.4.3 |
| 2 | **`Department.lead_id` 是否删除** | **建议删除**。部长身份只由 `Member.role = lead` 表达，避免同一事实存两份而不一致。需同步修订 `v1-scope.md` §5 |
| 3 | **成员名录的可见范围** | **全社团可见**（这是"社团里有哪些人"这个问题的答案）；任务与课题仍按部门限制 |
| 4 | **注册口令是否存明文** | **建议存明文**（仅会长 / 副会长可读）。它不是个人凭证，而是像群邀请码一样**需要被随时展示**的东西；存哈希会导致会长只能靠"重置"才能知道当前口令 |
| 5 | 部长能否审批定向到本部门的待分配账号 | v1 **先不做**（审批统一归会长 / 副会长）。若招新期仍是瓶颈，再作为增强——它能把审批压力真正下放 |
| 6 | 编辑成员姓名的权限 | 部长 / 副部长限本部门；跨部门与角色变更归社团级 |

---

# Part 4 · 任务

## 4.0 本部分范围

任务是本工具的核心——第 1 节的第二个问题「**每件事由谁负责、做到什么程度**」全部落在这一部分。

| 分组 | 内容 |
| --- | --- |
| 4.1 **「我的任务」首页** | v1 最重要的接口 |
| 4.2 任务列表 | 筛选与分页 |
| 4.3 任务详情 | |
| 4.4 创建任务 | |
| 4.5 编辑 / 转交 | |
| 4.6 状态流转与阻塞原因 | |
| 4.7 删除 | 软删除 |
| 4.8 **日历同步** | ⚠️ 数据模型需要修正，见 4.8 |
| 4.9 公共结构 | |
| 4.10 待拍板 | |

## 4.1 GET /tasks/mine —— 我的任务（首页）

**这是 v1 最重要的接口。** 第 1 节已经论证过：真正让人每天打开 App 的是「我负责什么」，不是组织架构图。

**权限**：全体 `active` 成员（永远只返回自己的任务）

**响应 200**——**服务端直接分好组**：

```json
{
  "ok": true,
  "data": {
    "overdue":  [ { "…TaskBrief…" } ],
    "blocked":  [ { "…TaskBrief…" } ],
    "due_today":[ { "…TaskBrief…" } ],
    "due_week": [ { "…TaskBrief…" } ],
    "later":    [ { "…TaskBrief…" } ],
    "no_due":   [ { "…TaskBrief…" } ],
    "counts": { "overdue": 2, "blocked": 1, "open_total": 7 }
  }
}
```

**为什么由服务端分组，而不是客户端算：**

1. 「今天 / 本周 / 逾期」的判定依赖**时区口径统一**（Part 1 §1.8）。客户端各自算，跨设备会不一致
2. 客户端少一份逻辑，就少一处 bug——这块逻辑每个客户端都要写一遍

**排序**：组内按 `due_at` 升序；`no_due` 排最后。

**⚠️「逾期」是派生量，不是状态。** 任务状态永远只有 4 档，`is_overdue` 由 `due_at` 与当前时间算出。
这与「课题不单独设状态」同源：**能从事实推导的东西就不要单独存**，否则两边必然不一致。

`blocked` 组里的任务同时也会出现在其他组（阻塞是状态，逾期是时间事实，两者不互斥）。

## 4.2 GET /tasks

**权限**：按 `view_scope`（会长/副会长 = 全社团；部长及以下 = 本部门）

**Query**

| 参数 | 说明 |
| --- | --- |
| `dept_id` | 按部门 |
| `owner_id` | 按负责人 |
| `status` | 4 档之一 |
| `plan_id` | 按课题（**含子课题**，见下） |
| `overdue` | `true` 只看逾期 |
| `include_subplans` | 默认 `true`；配 `plan_id` 用 |
| `page` / `size` | 见 Part 1 §1.7 |

> `plan_id` 默认**包含整棵子树**的任务。多级课题下，按父课题筛选却只看到本级任务，是不符合直觉的。

**响应 200**：分页的 `TaskBrief` 列表

## 4.3 GET /tasks/{id}

**权限**：按 `view_scope`；**成员可读本部门全部任务**（Part 1 §1.6）

**响应 200**：`TaskDetail`（见 4.9）

## 4.4 POST /tasks

**权限**：会长 / 副会长 / 部长 / 副部长

```json
{
  "title": "联系场地",
  "desc": "10 月活动场地，需确认容纳人数",
  "owner_id": 12,
  "due_at": "2026-09-20T18:00:00+08:00",
  "plan_id": 5,
  "client_token": "uuid"
}
```

| 字段 | 必填 | 说明 |
| --- | --- | --- |
| `title` | ✅ | |
| `owner_id` | ✅ | **且仅一人**——见 4.10 待拍板项 2 |
| `desc` | — | |
| `due_at` | — | 可空（进 `no_due` 组） |
| `plan_id` | — | 可挂到**任意层级**的课题；不挂则为独立任务。**课题所属部门必须与任务负责人同部门**（子课题按根课题算），否则 403 `FORBIDDEN_NOT_IN_DEPT`（N-18：否则 A 部门的任务会出现在 B 部门的课题详情里） |

**分配范围校验**：部长 / 副部长只能把任务分配给**本部门成员**；会长 / 副会长可跨部门。

| code | HTTP | 场景 |
| --- | --- | --- |
| `FORBIDDEN_NOT_IN_DEPT` | 403 | 把任务分给外部门的人 |
| `MEMBER_PENDING` | 403 | 目标成员尚未被分配部门与角色 |
| `MEMBER_DISABLED` | 403 | 目标成员已被移出社团（`PATCH /tasks/{id}` 的转交路径同样适用） |
| `VALIDATION_FAILED` | 400 | |
| `PLAN_NOT_FOUND` | 404 | |

## 4.5 PATCH /tasks/{id}

**权限**：创建人 / 部门管理员 / 会长 / 副会长

字段全部可选：`title` / `desc` / `owner_id` / `due_at` / `plan_id`
（`plan_id` 与 `owner_id` 同样受"负责人与课题必须同部门"约束，见 §4.1 的 `plan_id` 说明）

**改 `owner_id` 就是「转交」**，会更新 `updated_at`。

> ⚠️ `owner_id` **不能置空**。任务必须始终有人负责——这是整个工具的基本前提。

## 4.6 PUT /tasks/{id}/status

**权限**：负责人本人 / 部门管理员 / 会长 / 副会长

```json
{ "status": "blocked", "blocker": "等场地审批，已提交 3 天" }
```

| status | 说明 |
| --- | --- |
| `todo` | 未开始 |
| `doing` | 进行中 |
| `blocked` | 阻塞 |
| `done` | 已完成 |

**规则**

1. **进入 `blocked` 时必须带 `blocker`**，否则 `BLOCKER_REQUIRED`（400）。
   这是整个工具信息量最大的一个字段——它把「进度落后」翻译成「需要帮助」。允许留空就等于允许它变成摆设。
2. **离开 `blocked` 时自动清空 `blocker`**（历史由 `updated_at` 体现，v1 不做完整变更历史）
3. 进入 `done` 记 `completed_at`；从 `done` 退回其他状态时清空
4. **允许任意状态互跳，不设状态机流程图**。社团场景下限制跳转只会添堵——真实情况是"这事其实早做完了，我忘了标"

## 4.7 DELETE /tasks/{id}

**权限**：创建人 / 部门管理员 / 会长 / 副会长

**采用软删除**：标记 `deleted_at`，不再出现在任何列表，但数据保留。

> 理由是 `v1-scope.md` 规则 9 的同一条原则：**任务承载着人的工作量记录**。
> 硬删除会让"这学期我做了多少事"永远无法回答，而误删在移动端非常容易发生。

## 4.8 日历同步 —— ⚠️ 数据模型需要修正

`v1-scope.md` §7 把 `calendar_event_id` 放在**服务端的 `Task` 表**上。**这个位置是错的**，理由有四条：

1. **日历事件在成员自己的手机上**，不在服务端
2. **同一个任务可能被多个人加入日历**（负责人、部长、关注者），服务端存**一个** id 无法表达
3. 换手机后本地事件消失，服务端那个 id 就成了**悬空引用**
4. 服务端**不应该也不需要**知道谁把什么事加进了自己的日历——这本身是隐私

### 正确模型：事件 ID 由客户端本地维护

| 位置 | 存什么 |
| --- | --- |
| **客户端本地** | `task_id → { event_id, cached_due_at, cached_status }` |
| **服务端** | 只提供事实：`due_at`、`status`、`updated_at`。**不存任何日历字段** |

客户端在拉取任务时比对 `cached_*` 与服务器值：

| 差异 | 客户端动作 |
| --- | --- |
| `due_at` 变了 | **更新**本地日历事件（不是新建） |
| `status` 变为 `done` | **删除**本地日历事件 |
| 任务已不在服务器返回中 | **删除**本地日历事件 |
| 本地没有、服务器有 | 不动（用户没加过日历） |

### 为此需要一个辅助接口

客户端需要知道"我加过日历的任务，现在还在不在"。

### POST /tasks/lookup

**权限**：`active` 成员

```json
{ "task_ids": [12, 15, 27] }
```

**响应 200**

```json
{
  "ok": true,
  "data": {
    "items": [ { "id": 12, "due_at": "…", "status": "doing", "updated_at": "…" } ],
    "missing": [ 15, 27 ]
  }
}
```

`missing` 里的 id 表示**任务已删除或无权访问** → 客户端据此清理本地日历事件。

**说明**：这个接口很小，但没有它，客户端就无法可靠地回收日历事件，日历里会堆积错误提醒——这会让成员再也不相信它。

### 对 v1-scope 的影响

| 位置 | 需要修订 |
| --- | --- |
| `v1-scope.md` §5 `Task` 表 | **删除 `calendar_event_id` 字段** |
| `v1-scope.md` §7 | 从"服务端唯一的真工程量"改为**客户端机制** |

> **工作量没有消失，只是换到了客户端**。而且换过去之后**更好验证**——日历同步错了是用户看得见的，
> 藏在服务端反而难以发现。这一项需要在客户端排期里单独列出。

## 4.9 公共数据结构

### TaskBrief（列表用）

| 字段 | 类型 | 说明 |
| --- | --- | --- |
| `id` | int | |
| `title` | string | |
| `status` | string | `todo` / `doing` / `blocked` / `done` |
| `owner` | MemberBrief | 负责人 |
| `dept` | object \| null | 部门标签 |
| `plan` | object \| null | `{ "id": 5, "title": "…" }` |
| `due_at` | string \| null | ISO 8601 |
| `is_overdue` | bool | **服务端算好的派生量** |
| `blocked` | bool | |
| `updated_at` | string | **自动记录，不可由客户端设置** |

### TaskDetail

`TaskBrief` 的全部字段，外加：

| 字段 | 类型 | 说明 |
| --- | --- | --- |
| `desc` | string | 描述 |
| `blocker` | string \| null | 阻塞原因 |
| `plan_path` | array | 课题面包屑，从顶层到本级 |
| `created_by` | MemberBrief | |
| `created_at` | string | |
| `completed_at` | string \| null | |

> `updated_at` **必须由服务端生成**，不接受客户端传入。它是"做到什么程度"可信度的基础，
> 一旦允许客户端写，这个字段就失去意义了。

## 4.10 本部分待拍板

| # | 事项 | 我的建议 |
| --- | --- | --- |
| 1 | **`Task.calendar_event_id` 从服务端移除** | **强烈建议移除**，改由客户端本地维护（见 4.8）。同时修订 `v1-scope.md` §5 与 §7 |
| 2 | 一个任务的负责人数量 | **严格一人，不可置空**。多个负责人等于没人负责 |
| 3 | 状态是否允许任意互跳 | **允许**，不做状态机限制。现实是"早就做完了，忘了标" |
| 4 | 任务删除方式 | **软删除**，保留工作量记录 |
| 5 | 是否允许无截止时间的任务 | **允许**，归入 `no_due` 组 |
| 6 | 是否做完整的状态变更历史 | v1 **不做**；只保留 `updated_at` / `completed_at` 两个时间点 |
| 7 | 部长能否把任务分配给外部门的人 | **不能**；跨部门分配归会长 / 副会长 |

---

# Part 5 · 课题

## 5.0 本部分范围

课题是**可层层往下分割的容器**，任务是叶子。`v1-scope.md` 3.5.2 已经列明四个"必须做对"的点，全部落在这一部分：

| # | 必须做对的点 | 在哪一节 |
| --- | --- | --- |
| 1 | **环形引用校验** | 5.6 |
| 2 | **递归进度聚合** | 5.8 |
| 3 | **删除上提**（不级联） | 5.7 |
| 4 | **每个课题必须有负责人** | 5.4 |

## 5.1 树结构的两条数据规则

### 规则 A：`dept_id` 只存在**顶层课题**上

初稿给每个 `Plan` 都存 `dept_id`，并规定"子课题继承父课题"。但**"继承"如果靠在子节点复制一份，就又是同一事实存两份**——和 `Department.lead_id` 完全一样的问题。

**正确做法：子课题不存 `dept_id`。** 其部门由所属**根课题**决定。

这样做换来三个好处：

1. 「子课题不允许跨部门」**自动成立**，不需要写校验
2. **移动课题时不需要级联更新部门**——因为根本没存
3. 又消灭一类不一致

### 规则 B：深度硬上限 6 层

**创建与移动时校验**：超过 6 层返回 `PLAN_DEPTH_EXCEEDED` (400)。

环形校验防的是环，防不了"上千层直线嵌套"这类病态数据——**上限的意义是防呆，不是限制表达**。6 层足够覆盖任何真实的社团拆分（顶层课题 → 子课题 → 孙课题 …）。

`GET /plans` 的 `depth` 参数同样以 **6** 为上限。

> UI 上建议实践控制在 **3 层以内**——超过 3 层，横向空间和人的短期记忆都会崩，成员会找不到自己在哪。

## 5.2 GET /plans —— 课题树

**权限**：按 `view_scope`

**Query**

| 参数 | 说明 |
| --- | --- |
| `dept_id` | 只看某部门的树（按**根课题**的部门） |
| `depth` | 展开层数，默认全展开，**上限 6**（与 5.1 的深度硬上限一致；原写 10 属笔误） |
| `include_progress` | 默认 `true`；关掉可减少计算量 |

**响应 200**——返回**森林**（顶层节点数组），每个节点内嵌 `children`：

```json
{
  "ok": true,
  "data": {
    "items": [
      {
        "id": 5, "title": "2026 招新", "owner": { "…MemberBrief…" },
        "dept": { "id": 2, "name": "运营部" },
        "due_at": "2026-10-01T18:00:00+08:00",
        "progress": { "done": 3, "total": 8 },
        "task_count": 2, "child_count": 2,
        "children": [ { "…同结构…" } ]
      }
    ]
  }
}
```

不分页——社团规模的课题数量（几十个）一次返回更实用，也省掉客户端拼树。

## 5.3 GET /plans/{id}

**权限**：按 `view_scope`

**响应 200**

```json
{
  "ok": true,
  "data": {
    "plan": { "…PlanBrief…" },
    "path": [ { "id": 1, "title": "顶层课题" }, { "id": 5, "title": "…" } ],
    "children": [ { "…PlanBrief，不含 children…" } ],
    "tasks": { "items": [ { "…TaskBrief…" } ], "total": 5, "page": 1, "size": 50 }
  }
}
```

- `path` 是**面包屑**（从顶层到本级，不含自身或含自身由客户端决定，这里含自身）
- `tasks` **只返回本级任务**，不含子课题的任务——子课题的任务在各自的详情页里
- `progress` 仍是**递归聚合**值（含整棵子树）

## 5.4 POST /plans —— 创建课题 / 子课题

**权限**：会长 / 副会长（任意）；部长 / 副部长（本部门）

```json
{
  "title": "场地与物资",
  "desc": "…",
  "parent_id": 5,
  "dept_id": 2,
  "owner_id": 12,
  "due_at": "2026-09-25T18:00:00+08:00",
  "client_token": "uuid"
}
```

| 字段 | 必填 | 说明 |
| --- | --- | --- |
| `title` | ✅ | |
| `owner_id` | ✅ | **任意层级的课题都必须有负责人**（`v1-scope.md` 规则 14） |
| `parent_id` | — | 空 = **顶层课题**；有值 = **子课题** |
| `dept_id` | **仅顶层必填** | 子课题**不接受**该字段，部门继承自根课题（见 5.1 规则 A） |
| `due_at` | — | |

| code | HTTP | 场景 |
| --- | --- | --- |
| `VALIDATION_FAILED` | 400 | 顶层缺 `dept_id`；或子课题却传了 `dept_id` |
| `PLAN_NOT_FOUND` | 404 | `parent_id` 不存在 |
| `PLAN_DEPTH_EXCEEDED` | 400 | 超过 **6 层**深度上限 |

## 5.5 PATCH /plans/{id}

**权限**：课题负责人 / 部门管理员 / 会长 / 副会长

字段全部可选：`title` / `desc` / `owner_id` / `due_at`

> ⚠️ **`owner_id` 不能置空**——同任务，课题必须始终有人负责。
> ⚠️ **`dept_id` 不可通过此接口修改**（子课题不存该字段；顶层若要换部门，用 5.6 的移动语义或单独确认）。
> ⚠️ **响应里的 `progress` 必须是真实聚合值**（2026-09-14 修订，见 `code-review.md` M-1）。
> 该字段与 `POST /plans` 的响应原先走的是"不算进度"的分支，序列化出来恒为 `total: 0 / done: 0`；
> 客户端改一次标题，进度条就归零——一个看起来像数据丢失的假象。
> 凡返回课题对象的地方（详情、创建、编辑、移动、幂等回放）一律取真实聚合值。

## 5.6 POST /plans/{id}/move —— ⚠️ 环形校验

**权限**：会长 / 副会长；部长 / 副部长限本部门

```json
{ "new_parent_id": 3 }
```

`new_parent_id` 为 `null` 表示**提升为顶层课题**。

**校验（按顺序）**

| # | 校验 | 失败错误码 |
| --- | --- | --- |
| 1 | `new_parent_id != id`（不能挂到自己下面） | `PLAN_CYCLE_DETECTED` (409) |
| 2 | `new_parent_id` **不能是自身的后代** | `PLAN_CYCLE_DETECTED` (409) |
| 3 | `new_parent_id` 存在且未删除 | `PLAN_NOT_FOUND` (404) |
| 4 | 提升为顶层时，需要确定 `dept_id` | `VALIDATION_FAILED` (400) |
| 5 | **目标部门也要判权限**（提升为顶层时带 `dept_id` 的情形） | `FORBIDDEN_NOT_IN_DEPT` (403) |
| 6 | **禁止跨部门移动**（换父节点与提升为顶层两条路都禁止） | `VALIDATION_FAILED` (400) |

> ⚠️ **校验 5 是 2026-09-14 补的 P0 缺陷修复（见 `code-review.md` H-3）**。
> 换父节点那条路先判了"目标父节点所在部门"的权限，但**提升为顶层那条路原先没有判**：
> 部长可以把自己部门的课题 `dept_id` 一填就"提升"到别的部门去。
> 现在两条路都判：先按操作真正落地的部门跑一次权限，再执行"跨部门禁止"的一致性检查。
> 顺序上权限在前——否则会先给出"v1 不允许跨部门"这种与调用者权限无关的提示。

### 环形校验的实现提示

**从目标父节点向上遍历到根**，而不是向下扫子树：

```
cur = new_parent_id
while cur != null:
    if cur == id:  -> 拒绝 PLAN_CYCLE_DETECTED
    cur = parent_of(cur)
```

向上遍历的深度通常远小于子树规模，且实现简单。**这是多级结构下最容易出的事故**——一旦成环，5.8 的递归进度聚合会无限循环并拖垮服务。

### 移动为什么不需要级联更新部门

因为 `dept_id` 只存在顶层（5.1 规则 A）。把一棵子树挂到另一个根下，它的部门**自动**跟随新根，无需 UPDATE 任何子节点。

## 5.7 DELETE /plans/{id} —— 上提一级

**权限**：部门管理员 / 会长 / 副会长

**语义：删除课题 P 时，对 P 的「直接子节点」执行上提，绝不级联删除。**

| 子节点类型 | 上提后 |
| --- | --- |
| 子课题 | `parent_id = P.parent_id` |
| 本级任务 | `plan_id = P.parent_id` |

**P 是顶层课题时**（`P.parent_id` 为空）：

- 子课题变为**顶层课题**，并**继承 P 的 `dept_id`** ← 因为 `dept_id` 只存在顶层，这一步不能漏
- 本级任务变为**独立任务**（`plan_id = null`）

**响应 200**

```json
{ "ok": true, "data": { "moved_plans": 2, "moved_tasks": 5 } }
```

> **为什么不做级联删除：** `v1-scope.md` 规则 9——任务承载着人的工作量记录，不能因为删一个容器就消失。
> 一次误删丢掉整棵子树，是这个设计里最不可接受的失败模式。
>
> 整个上提操作必须**原子完成**，不能出现"子课题提上去了、任务还挂在已删课题下"的中间态。

## 5.8 递归进度聚合

```
progress(P) = 
    done  = P 本级已完成任务数 + Σ done(子课题)
    total = P 本级任务总数   + Σ total(子课题)
```

**含所有后代任务**，不只是直接下挂的。

**实现提示（很重要）**：构建树时**自底向上一次算完**，不要对每个节点独立递归。

- ✅ 一次后序遍历，O(n)
- ❌ 每个节点各自递归 → O(n²)，课题一多就明显变慢

在 `GET /plans` 返回整棵树时，这个差别从"几十个课题"开始就能感觉到。

> `Plan` **不设 status 字段**——进度完全是派生的（`v1-scope.md` 规则 7）。
> 这样从结构上杜绝了「任务全完成了、课题还写着进行中」这类不一致，不需要靠人去维护。

## 5.9 公共数据结构

### PlanBrief

| 字段 | 类型 | 说明 |
| --- | --- | --- |
| `id` | int | |
| `title` | string | |
| `owner` | MemberBrief | **必填** |
| `dept` | object \| null | **仅顶层课题有值**；子课题为 `null`，部门看根的 |
| `due_at` | string \| null | |
| `progress` | object | `{ "done": 3, "total": 8 }` —— **递归聚合，含整棵子树** |
| `task_count` | int | **本级**任务数（不含子课题） |
| `child_count` | int | 直接子课题数 |
| `children` | array | 仅 `GET /plans` 返回；详情页用平铺的 `children` |
| `created_at` / `updated_at` | string | |

## 5.10 决策

### 已确认（2026-09-13）

| # | 事项 | 结论 |
| --- | --- | --- |
| 1 | `Plan.dept_id` 只在顶层 | **采纳**（见 5.1 规则 A），已同步 `v1-scope.md` v0.8 |
| 2 | 课题深度硬上限 | **6 层**（`PLAN_DEPTH_EXCEEDED`） |
| 3 | 移动课题能否跨部门 | **禁止**。采纳项 1 后，「跨部门」等价于「换根」，保持部门边界清晰 |
| 4 | `GET /plans` 是否分页 | **不分页**，一次返回整棵树 |
| 5 | 顶层课题能否换部门 | v1 **不做** |
| 6 | 任务能否直接挂在顶层课题上 | **可以**，任务可挂任意层级 |

## 5.11 实现补充（2026-09-13）

> 服务端 M4 落地时明确的口径，均为**细化**而非改语义；第 1 条是修正文档自身的矛盾。

| # | 事项 | 口径 |
| --- | --- | --- |
| 1 | `GET /plans` 的 `depth` 上限 | **6**（§5.1 早已写 6，§5.2 表格误写 10）。超过 6 无意义——树本身就有 6 层硬上限 |
| 2 | `include_progress=false` | **省略 `progress` 字段**（该参数存在的意义就是省掉聚合计算），`task_count` / `child_count` 照常返回 |
| 3 | 超出 `depth` 的层级 | 不展开 `children`（给空数组），但 `child_count` 照常返回，客户端据此知道"下面还有" |
| 4 | `move` 提升为顶层时的部门 | 可用请求体里的 `dept_id` 指定；不传则**沿用原来的有效部门**。两者都拿不到部门时才是 `VALIDATION_FAILED`（对应 §5.6 校验 4） |
| 5 | 移动时的深度校验 | `新父节点深度 + 被移动子树高度 ≤ 6`，否则 `PLAN_DEPTH_EXCEEDED`——只算被移动的那棵子树 |
| 6 | 跨部门移动 | 一律 `VALIDATION_FAILED`（§5.10 决策 3）；部长/副部长若目标在本部门之外则是 `FORBIDDEN_NOT_IN_DEPT` |
| 7 | 写接口的响应形状 | `POST /plans`、`PATCH /plans/{id}`、`POST /plans/{id}/move` 一律把课题对象放在 **`data.plan`** 下，与 `GET /plans/{id}` 一致 |
| 8 | `DELETE /plans/{id}` 的计数 | `moved_tasks` 只计**未删除**的任务；已软删除的任务同样会被改引用（避免悬空 id），但不计数 |
| 9 | 幂等 | 创建课题同样支持 `client_token`（§5.4 已列该字段），命中时返回 200 并带 `duplicated: true` |

---

# Part 6 · 交叉复查

## 6.0 本部分目的

**不新增任何功能。** 前三部分是我分块写的，这一部分用全局视角做三张对照表，检查：

1. 所有接口是否都被页面**用得上**（有没有白写的接口）
2. 所有页面是否都被接口**支撑得住**（有没有做不出来的页面）
3. v1 的业务规则是否都有**接口落点**（有没有写了却无人执行的规则）
4. 权限定义在各部分之间**是否自相矛盾**

## 6.1 接口总清单（39 个）

### 认证（5）

| 方法 | 路径 | 权限 | Part |
| --- | --- | --- | --- |
| POST | `/auth/register` | 公开（需注册口令） | 2 |
| POST | `/auth/login` | 公开 | 2 |
| POST | `/auth/logout` | 已登录 | 2 |
| GET | `/auth/me` | 已登录 | 2 |
| PUT | `/auth/password` | 本人 | 3 |

### 组织与成员（19）

| 方法 | 路径 | 权限 | Part |
| --- | --- | --- | --- |
| GET | `/depts` | 全体 active | 3 |
| POST | `/depts` | **仅会长** | 3 |
| PATCH | `/depts/{id}` | **仅会长** | 3 |
| DELETE | `/depts/{id}` | **仅会长** | 3 |
| GET | `/members` | 全体 active（全社团） | 3 |
| GET | `/members/{id}` | 全体 active | 3 |
| PATCH | `/members/{id}` | 见 3.8 | 3 |
| POST | `/members/{id}/disable` | 会长 / 副会长 | 3 |
| GET | `/members/pending` | 会长 / 副会长 | 3 |
| POST | `/members/{id}/assign` | 会长 / 副会长（**仅限档位低于自己者**） | 3 |
| POST | `/members/assign-batch` | 会长 / 副会长（**仅限档位低于自己者**） | 3 |
| POST | `/members/{id}/transfer-presidency` | **仅会长** | 3 |
| POST | `/members/{id}/reset-password` | 会长 / 副会长 / 本部门部长 / 副部长 | 3 |
| GET | `/register-config` | 会长 / 副会长 | 3 |
| PUT | `/register-config/code` | 会长 | 3 |
| POST | `/register-config/rotate` | 会长 | 3 |
| GET | `/dept-invite-links` | 会长 / 副会长 | 3 |
| POST | `/dept-invite-links` | 会长 / 副会长 | 3 |
| DELETE | `/dept-invite-links/{token}` | 会长 / 副会长 | 3 |

### 任务（8）

| 方法 | 路径 | 权限 | Part |
| --- | --- | --- | --- |
| GET | `/tasks/mine` | 本人 | 4 |
| GET | `/tasks` | 按 view_scope | 4 |
| GET | `/tasks/{id}` | 按 view_scope | 4 |
| POST | `/tasks` | 会长 / 副会长 / 部长 / 副部长 | 4 |
| PATCH | `/tasks/{id}` | 创建人 / 部门管理员 / 会长 / 副会长 | 4 |
| PUT | `/tasks/{id}/status` | 负责人 / 部门管理员 / 会长 / 副会长 | 4 |
| DELETE | `/tasks/{id}` | 创建人 / 部门管理员 / 会长 / 副会长 | 4 |
| POST | `/tasks/lookup` | 全体 active（客户端同步用） | 4 |

### 课题（6）

| 方法 | 路径 | 权限 | Part |
| --- | --- | --- | --- |
| GET | `/plans` | 按 view_scope | 5 |
| GET | `/plans/{id}` | 按 view_scope | 5 |
| POST | `/plans` | 会长 / 副会长 / 部长 / 副部长 | 5 |
| PATCH | `/plans/{id}` | 负责人 / 部门管理员 / 会长 / 副会长 | 5 |
| POST | `/plans/{id}/move` | 部门管理员 / 会长 / 副会长 | 5 |
| DELETE | `/plans/{id}` | 部门管理员 / 会长 / 副会长 | 5 |

### 运维（1）

| 方法 | 路径 | 权限 |
| --- | --- | --- |
| GET | `/health` | 公开 |

## 6.2 页面 → 接口映射

| # | 页面 | 依赖接口 |
| --- | --- | --- |
| 1 | **我的任务**（首页） | `GET /tasks/mine`、`PUT /tasks/{id}/status`、`GET /auth/me` |
| 2 | 任务列表 | `GET /tasks`、`POST /tasks`、`GET /depts`、`GET /plans`（筛选）、`GET /members`（按人筛选） |
| 3 | 任务详情 | `GET /tasks/{id}`、`PATCH /tasks/{id}`、`PUT /tasks/{id}/status`、`DELETE /tasks/{id}`、`POST /tasks/lookup` |
| 4 | 课题列表（树） | `GET /plans`、`POST /plans`、`POST /plans/{id}/move`、`DELETE /plans/{id}` |
| 5 | 课题详情 | `GET /plans/{id}`、`PATCH /plans/{id}`、`GET /tasks?plan_id=` |
| 6 | 成员名录 | `GET /members`、`GET /members/{id}`、`GET /depts` |
| 7 | 加入流程 | `POST /auth/register`（+ 招募链接落地带入 `dept_id`） |
| 8 | 待分配审批 | `GET /members/pending`、`POST /members/{id}/assign`、`POST /members/assign-batch` |
| 9 | 管理 | `GET/POST/PATCH/DELETE /depts`、`PATCH /members/{id}`、`POST /members/{id}/disable`、`POST /members/{id}/transfer-presidency`、`POST /members/{id}/reset-password`、`GET/PUT/POST /register-config`、`GET/POST/DELETE /dept-invite-links` |

**→ 发现缺口 A：页面清单漏了 2 页**（详见 6.5）

## 6.3 业务规则 → 接口落点

`v1-scope.md` §6 共 19 条规则，逐条核对：

| # | 规则 | 由谁保证 |
| --- | --- | --- |
| 1 | 任务只有一个负责人 | `POST /tasks`（必填单一）、`PATCH /tasks/{id}`（不可置空） |
| 2 | 状态只有 4 档 | `PUT /tasks/{id}/status` 枚举校验 |
| 3 | 阻塞原因 | `PUT /tasks/{id}/status` → `BLOCKER_REQUIRED` |
| 4 | `updated_at` 自动记录 | 全部写接口，**服务端生成** |
| 5 | 角色固定 5 档 | `PATCH /members/{id}`、`POST /members/{id}/assign` |
| 6 | 人数只做软提示 | `GET /depts` 返回 `member_count`，**不做校验** |
| 7 | 课题进度递归聚合 | `PlanBrief.progress`（§5.8） |
| 8 | 任务可不属于课题 | `POST /tasks` 的 `plan_id` 可空；**挂了课题则课题部门必须与任务负责人同部门**（N-18） |
| 9 | 删除课题上提一级 | `DELETE /plans/{id}`（§5.7） |
| 10 | 至少一个 `president` | `PATCH /members/{id}`、`POST /members/{id}/disable`、`transfer-presidency` → `FORBIDDEN_LAST_PRESIDENT` |
| 11 | 会长移交是原子操作 | `POST /members/{id}/transfer-presidency` |
| 12 | 注册与授权分离 | `POST /auth/register`（产 `pending`）+ `assign` |
| 13 | 课题树禁止环形 | `POST /plans/{id}/move` → `PLAN_CYCLE_DETECTED` |
| 14 | 任意层级课题必须有负责人 | `POST /plans` 的 `owner_id` 必填 |
| 15 | 逾期是派生量 | `GET /tasks/mine`、`TaskBrief.is_overdue` |
| 16 | `updated_at` 服务端生成 | 全部写接口 |
| 17 | 任务软删除 | `DELETE /tasks/{id}` → `deleted_at` |
| 18 | 课题深度上限 6 层 | `POST /plans`、`POST /plans/{id}/move` → `PLAN_DEPTH_EXCEEDED` |
| 19 | `dept_id` 只在顶层 | `POST /plans`（子课题拒绝该字段） |

**结论：19 条规则全部有落点，无遗漏。**

## 6.4 权限对照复查

把 Part 3 §3.8 的权限表与 Part 4 / Part 5 的接口权限逐行比对：

| 检查项 | 结果 |
| --- | --- |
| 「查看成员名录 = 全社团」是否一致 | ✅ 三处一致 |
| 「查看任务/课题 = 按 view_scope」是否一致 | ✅ |
| 「创建任务 = 会长/副会长/部长/副部长」 | ✅ |
| 「更新任务状态 = 成员限自己」 | ✅ |
| 「移交会长 = 仅会长」 | ✅ |
| 「重置密码 = 部长限本部门」 | ✅ |
| 「注册口令 = 副会长仅查看」 | ✅（`rotate` 与 `PUT /code` 均限会长） |
| 「部门增删改 = 仅会长」 | ✅ 三处已一致（§3.1 / §3.8 / §6.1，2026-09-13 修订） |

**→ 发现缺口 B：权限矩阵缺 3 类行**（详见 6.5）

## 6.5 缺口与遗留清单

### 缺口 A：页面清单漏了 2 个页面

`v1-scope.md` §4 列了 9 个页面，但以下接口**没有对应页面**：

| 无落点的接口 | 应该属于 |
| --- | --- |
| `POST /auth/login` | **登录页** |
| `POST /auth/register` | 注册（目前只挂在"加入流程"，但它是独立入口） |
| `POST /auth/logout` | **我的 / 个人设置页** |
| `PUT /auth/password` | **我的 / 个人设置页** |
| `GET /auth/me` 的展示 | **我的 / 个人设置页** |

**建议：页面清单从 9 页改为 11 页**，补上：

10. **登录页**（含注册入口、忘记密码提示）
11. **我的 / 个人设置**（本人信息、改密码、退出登录）

> 这不是可有可无的细枝末节——**成员每次打开 App 都会经过登录页**，而"退出登录在哪"是用户遇到问题时的第一反应。

### 缺口 B：权限矩阵缺 3 类行

`v1-scope.md` §3.4.3 的矩阵有「创建课题」「创建/分配任务」「更新任务状态」，但 Part 4 / Part 5 还定义了这些权限，**矩阵里没有对应行**：

| 缺失的能力 | 实际权限（Part 4/5 已定义） |
| --- | --- |
| **编辑任务内容**（`PATCH /tasks/{id}`） | 创建人 / 部门管理员 / 会长 / 副会长 |
| **删除任务**（`DELETE /tasks/{id}`） | 创建人 / 部门管理员 / 会长 / 副会长 |
| **编辑 / 移动 / 删除课题**（`PATCH`、`move`、`DELETE /plans`） | 负责人 / 部门管理员 / 会长 / 副会长 |

**建议给矩阵补这 3 行。** 不补的话，实现时这几处权限会各自为政——而权限不一致是最难在测试中发现的一类缺陷。

### 说明：两个"不对应页面"的接口（不构成缺口）

| 接口 | 为什么没有页面 |
| --- | --- |
| `POST /tasks/lookup` | 客户端**后台同步**用（日历比对），用户不直接调用 |
| `GET /health` | 运维 / 部署健康检查 |

列出它们是**避免复查时被误当成遗漏**。

## 6.6 文档状态

| 文档 | 版本 | 状态 |
| --- | --- | --- |
| `v1-scope.md` | **v0.11** | 范围基准，6.5 的两处缺口已补（v0.9/v0.10）；v0.11 为 M1 实现期间的三处修订 |
| `api-design.md` | Part 1–6 完成 | **接口设计 39 个**，错误码/权限已与实现对齐；2026-09-13 修订部门权限与会长产生路径 |
| `deploy-windows-verify.md` | — | 服务端部署与验证（Windows），**待你跑步骤 0** |
| `frontend-brief.md` | v1.0 | 前端对接说明（精简版） |
| *对外材料（已移出仓库）* | — | `qingzhou-tls-requirement.md`（已发轻舟团队）、`qingzhou-tls-verification.md`（已验证他们交付的 TLS）—— 见上层 `cangjie-upstream\` |
| `server-guide.md` | — | **服务端**：构建/运行/测试与设计约定 |
| `API-NOTES.md` | — | **服务端**：编译期 API 事实清单与踩坑记录（探针实测） |

### 下一步建议

1. **先补 6.5 的两处缺口**（页面清单、权限矩阵），我可以立刻改 `v1-scope.md`
2. **跑服务器步骤 0**（10 分钟）——它是整个服务端路线的前提
3. 之后就可以开始写代码了：接口冻结、数据模型冻结、技术栈冻结、部署方式已定
